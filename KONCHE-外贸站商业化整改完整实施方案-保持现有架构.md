# KONCHE 外贸站商业化整改完整实施方案

版本：2026-08-26  
适用项目：KONCHE 外贸本地版  
核心原则：保留现有网站架构、页面风格和 CTA 文案，以底层改造、内容复用和轻量增强完成商业闭环。

## 一、项目边界

### 保持不变

- 主导航、页脚、首页主要区块和产品页面总体结构不大改。
- 现有 CTA 的可见文案原则上不改，例如 `Request a Project Quote`、`Discuss Your Requirement`、`Get Expert Support`。
- 不在首页和产品页增加大段正文，不破坏当前扫描式阅读体验。
- 继续使用现有“首屏简洁信息 + 技术卡片/表格 + 折叠文档 + FAQ”的双层结构。
- 不把网站改造成传统产品商城，不展示不可靠的固定价格和标准化交期。

### 允许调整

- CTA 的目标地址、提交方式、追踪参数和 CRM 路由可以调整，但按钮文字和视觉位置不变。
- 可在现有社交悬浮栏、页脚、联系页侧栏中补充 WhatsApp、国际电话和域名邮箱，不额外制造大型区块。
- 可替换或压缩重复文字，用真实项目证据替代泛泛宣传。
- 可在现有 `industry-solutions.html`、`spare-parts-sourcing-service.html`、`resources.html` 和联系页中增加紧凑模块或锚点。
- 只在确有搜索需求和商业路径差异时增加少量页面。

## 二、目标商业路径

网站同时承接三类线索，但不改变现有主视觉：

1. **标准设备快速询价**：产品页 → 保留原 CTA → 联系页预选产品 → 表单/WhatsApp/电话。
2. **高价值工程项目**：行业/解决方案/案例 → 原 CTA → 工程 RFQ → 上传水质报告 → CRM 高优先级分配。
3. **长期渠道合作**：OEM/经销商/耗材批发入口 → 现有联系页动态表单 → 渠道 CRM 队列。

核心不是增加更多按钮，而是让现有按钮根据来源页、产品、客户角色和 URL 参数进入不同的后端处理流程。

## 三、P0：域名与搜索资产整改

### 3.1 域名架构

推荐顺序：

1. 独立英文品牌域名；
2. `en.konchewater.com`；
3. `konchewater.com/en/`，仅在两套站点能够统一发布、重定向和语言路由时采用。

中国站继续使用 `konchewater.com`，英文站不得继续把 canonical 指向中文首页。

### 3.2 建立单一站点配置

当前大量 HTML、JSON-LD、sitemap 和工具脚本直接写死 `https://www.konchewater.com`。整改时不要手工逐页替换，而应增加一个站点配置源，例如：

- `SITE_ORIGIN`
- `SITE_NAME`
- `SALES_EMAIL`
- `SALES_PHONE`
- `WHATSAPP_NUMBER`
- `DEFAULT_LANGUAGE`
- `MARKETS`

使用构建/发布脚本统一生成或校验：

- canonical；
- Open Graph URL 和图片；
- JSON-LD 的 `@id`、`url`、logo 和 ContactPoint；
- sitemap URL；
- robots sitemap 地址；
- `llms.txt` 与 `llms-full.txt` 链接；
-部署检查基准域名。

### 3.3 URL 与重定向

- 为最终英文域名启用 HTTPS、单一主机版本和 301 收敛。
- 若曾公开使用 Workbuddy 或临时 URL，不把临时链接写入 canonical。
- URL 保持现有英文短路径，不因整改大规模改名。
- 需要改名的旧路径必须提供一对一 301，不重定向到首页。
- canonical、sitemap、内部链接和最终 200 URL 必须完全一致。

### 3.4 验收标准

- 任一正式页面只有一个正确 canonical。
- sitemap 不含重定向、404、Workbuddy 或中文站 URL。
- JSON-LD 不再引用错误域名。
- HTTP、HTTPS、www、非 www 最终只落到一个主机。
- 四件套全部通过，`deploy_check` 返回明确 PASS。

## 四、P0：保留现有表单外观，替换提交底层

### 4.1 不修改表单主体和 CTA 文案

继续使用现有 `contact.html#rfqForm` 的字段与视觉结构。将 `site-enhancements.js` 中的 mailto 提交替换为真实接口：

```text
浏览器表单
→ POST /api/inquiries
→ 服务器验证
→ 保存线索与附件
→ 发送销售邮件
→ 写入 CRM
→ 返回 inquiry_id
→ 页面显示成功状态
```

无需新建大型感谢页；可直接复用表单区域显示成功卡片。为广告归因可保留一个轻量 `/thank-you.html`，但默认体验以当前页成功状态为主。

### 4.2 自动采集，不增加客户填写负担

后端自动保存：

- 来源页面 URL；
- 来源产品；
- referrer；
- UTM source/medium/campaign/content/term；
- 首次访问时间和提交时间；
- 浏览器语言；
- 国家/地区推断值，客户填写值优先；
- 表单版本；
- consent 版本；
- WhatsApp、电话、邮箱或表单的渠道类型。

不要求客户手工填写这些字段。

### 4.3 根据来源页预填

保持 CTA 文案不变，只调整链接参数，例如：

```text
contact.html?product=containerized-ro
contact.html?product=ultrapure-water
contact.html?role=epc
contact.html?role=oem
contact.html?role=distributor
contact.html?role=consumables-wholesale
```

联系页读取参数后预选现有下拉项，并在 CRM 中进入相应队列。客户仍可修改。

### 4.4 失败兜底

- API 正常：直接提交并展示询盘编号。
- API 暂时失败：保留当前 mailto 作为二级兜底，而不是主要流程。
- CRM 暂时失败：后端先可靠保存，再通过队列重试 CRM。
- 邮件失败：不丢失数据库中的询盘，并触发内部告警。

### 4.5 验收标准

- 客户无需安装邮件客户端即可提交。
- 销售邮件、CRM 和数据库三处数据一致。
- 重复点击不会生成多条重复询盘。
- 成功后显示询盘编号、响应时间承诺和备用联系方式。
- 所有现有 CTA 都能带入正确来源产品。

## 五、P0：水质报告真实上传

### 5.1 保留当前文件选择控件

继续使用现有 PDF、Word、Excel、JPG、PNG 接受范围和 10 MB 提示，不改变表单布局。

### 5.2 上传流程

推荐使用对象存储的预签名上传：

1. 前端请求一次性上传地址；
2. 文件直接上传到私有存储；
3. 表单提交附件 ID，而不是传本地文件名；
4. CRM 和销售邮件收到安全下载链接；
5. 链接设有效期和访问权限。

### 5.3 安全要求

- 服务器校验扩展名、MIME、文件头和大小。
- 文件名随机化，不允许客户文件名直接成为存储路径。
- 病毒/恶意文件扫描。
- 上传目录不得执行脚本。
- 设置数据保留周期和人工删除能力。
- 隐私政策说明文件用途、保存时间和访问人员。

### 5.4 验收标准

- CRM 中能看到并下载真实附件。
- 超限、伪造格式和恶意文件被拒绝。
- 提交失败时孤立附件能定时清理。
- 未经授权不能通过公开 URL 获取报告。

## 六、海外联系渠道：利用现有位置，不新增大型 CTA

### 6.1 放置位置

复用以下位置：

- 已有社交悬浮栏：增加 WhatsApp 与国际电话图标；
- 联系页 `contact-direct`：补充电话、WhatsApp、时区和语言；
- 页脚 CONTACT 列：由 Outlook 邮箱改为域名邮箱，并补充电话/WhatsApp；
- 移动端：使用紧凑的底部或悬浮联系方式，不遮挡正文和表单。

现有按钮文案不改。

### 6.2 域名邮箱

使用：

- `sales@英文域名`：普通询盘；
- `projects@英文域名`：工程项目；
- `partners@英文域名`：OEM/经销商；
- `parts@英文域名`：耗材与备件。

可以在后台转发到同一团队，但对外必须保持品牌域名。配置 SPF、DKIM、DMARC，避免询盘回复进入垃圾箱。

### 6.3 WhatsApp 追踪

WhatsApp URL 自动带入简短预填内容：

```text
Hello KONCHE, I am viewing [Product/Page].
Country: [known value]
Reference: [short tracking id]
```

不在消息里暴露完整 UTM 或技术参数。点击事件写入分析系统；真正发送仍由客户确认。

### 6.4 电话

- 使用可国际拨打的 E.164 格式。
- 移动端使用 `tel:`。
- 标明服务时间、时区和语言。
- 不承诺无法持续执行的 7×24 人工响应。

## 七、响应承诺：替换现有短文，不增加新模块

不新增长段落，只在现有以下位置统一一行承诺：

- 联系页 hero 或 `fast-quote`；
- 产品页底部询盘区；
- 表单提交成功状态；
- WhatsApp 自动回复。

建议按可执行能力写成：

```text
Sales response within 24 hours (China time, UTC+8).
English and Chinese support, China Standard Time (UTC+8).
```

最终数字由销售团队确认。CRM 自动计算首响时间，无法持续达标就必须调整承诺。

## 八、信任资产：复用现有 About、Selected Projects 和产品页

### 8.1 不在首页堆放 6–10 个长案例

首页现有 `Selected Projects` 只保留 3–4 个最强案例卡，每个卡片显示：

- 国家/地区；
- 行业；
- 原水；
- 处理能力；
- 目标水质；
- 投运结果或交付状态；
- 一张真实图片。

点击后进入案例详情或 `resources.html` 中的案例条目。首页不增加大段叙述。

### 8.2 案例详情采用固定短模板

每个案例只需：

1. 80–120 字项目摘要；
2. 六项事实卡；
3. 一张工艺流程图；
4. 3–6 张真实图片；
5. 设备范围与责任边界；
6. FAT/出厂/投运结果；
7. 客户授权范围。

复杂工程内容放入 `<details>` 的 Project documentation，不占用首屏。

### 8.3 About 页面

在现有页面中替换泛化能力描述，增加可核验内容：

- 公司法定名称；
- 成立年份；
- 工厂地址和面积；
- 主要制造/测试能力；
- 认证证书编号和有效期；
- 专利号；
- FAT 能力；
- 出口市场和交付范围。

认证图片使用可放大查看，不把证书文字重复写成长文。

### 8.4 声明真实性

- “1000+客户”“20+国家”“安装缩短60%”等数字必须有内部证据和适用条件。
- 客户名称、Logo和现场照片必须确认授权。
- 无法公开客户名称时使用行业、国家和项目参数匿名展示。

## 九、行业客户页面：少增页面，先复用 industry-solutions

### 9.1 第一阶段：在现有页面增加四个紧凑锚点

在 `products/industry-solutions.html` 增加：

- Drinking Water Bottling；
- Agriculture & Irrigation；
- Construction Sites & Camps；
- Industrial Process Water。

每个锚点使用同一紧凑结构：

- 一句客户问题；
- 3个典型水源/场景；
- 3个推荐系统链接；
- 4项采购输入；
- 一个真实案例或“案例准备中”；
- 继续复用页面原有 CTA。

每个区块的直接可见正文控制在约 120–180 个英文单词。更完整技术说明进入已有折叠文档或 `llms-full.txt`。

### 9.2 第二阶段：根据数据决定是否拆页

只有同时满足以下条件才创建独立页面：

- 搜索或广告有明确流量；
- 与其他行业的决策参数明显不同；
- 有真实案例或独特能力；
- 页面能产生独立询盘或辅助转化。

没有证据时不一次性创建大量薄页面。先观察锚点点击、停留、搜索词和询盘来源，再决定拆分。

## 十、OEM、经销商和耗材批发：复用现有客户路径

### 10.1 首页

复用现有 `Who We Support`/`partner-pathways` 卡片，不增加新大区块。将卡片扩展为：

- EPC & Engineering Integrators；
- Industrial End Users；
- OEM & Distribution Partners。

如果不希望改卡片文字，可只在卡片详情、链接参数和联系页角色选项中增加渠道客户。

### 10.2 OEM 与经销商

不急于增加长页面。优先在现有 `about.html` 或 `resources.html` 增加一张紧凑的 Partner Capability 卡，并在联系页增加角色选项：

- OEM / Private Label Buyer；
- Distributor / Local Integrator。

选择后动态显示少量对应字段：

- 国家与销售区域；
- 关注产品；
- 年度预计采购量；
- 是否具有安装/售后能力；
- OEM、认证或区域合作需求。

CRM 分配到 `partners` 队列。

### 10.3 耗材批发商

直接复用现有：

- `products/spare-parts.html`；
- `products/general-water-treatment-consumables.html`；
- `products/uv-disinfection-consumables.html`；
- `products/spare-parts-sourcing-service.html`。

只增加：

- 批发商角色；
- 品类/品牌/型号；
- 数量和采购频率；
- 包装/OEM要求；
- 目的国。

原 CTA 不改，链接进入带 `role=consumables-wholesale` 的联系页或复用现有备件表单。

## 十一、GEO 与 AI：采用安全机器入口，不使用 cloaking

### 11.1 可以使用的“低干扰 AI 入口”

- `llms.txt`：核心公司、产品、案例和联系方式目录；
- `llms-full.txt`：完整技术摘要和引用入口；
- XML sitemap；
- robots 中明确允许的 AI crawler 和 sitemap；
- JSON-LD：Organization、WebSite、WebPage、Product/Service、FAQ、Breadcrumb；
- `<link rel="alternate" type="text/plain" href="/llms-full.txt">`；
- `<link rel="sitemap" type="application/xml" href="/sitemap.xml">`；
- 当前已有 `<details>` 技术文档；
- 页面中自然存在但视觉低权重的 Resources/Documentation 链接；
- 可公开访问的案例、规格和知识页面。

这些入口不会干扰用户，也不会向机器展示与人类不同的事实。

### 11.2 不建议的隐藏方式

不要使用：

- `display:none` 的关键词和链接堆；
- 将链接移到屏幕外；
- 字体颜色与背景相同；
- 只根据 User-Agent 给 AI/搜索爬虫返回额外内容；
- 人看不到、爬虫能看到的独立销售声明；
- 隐藏的大量交叉链接网络。

这些做法可能被视为 cloaking、隐藏文本或操纵排名，也会造成内容真实性和维护风险。

### 11.3 推荐的双层内容规则

- 首屏和主要区块服务人类阅读。
- 每页只保留一个直接 GEO 答案，不重复定义。
- 可引用技术正文放入当前已有的 documentation `<details>`。
- AI 目录通过 `llms.txt`、sitemap、JSON-LD 和 `<link rel>` 发现。
- AI 内容与用户展开后看到的内容完全一致。
- 折叠内容不能只是关键词，必须是真实、独特、可核验的技术说明。

## 十二、可读性保护线

整改不得降低当前用户体验，执行以下硬规则：

- Hero 主体不超过一个 H1、一个短段落、现有 CTA 和 3–4 个关键事实。
- 首屏与直接答案区可见段落总量维持当前审计上限。
- 每个新增行业锚点最多一个短段落，不在连续卡片下重复公司介绍。
- 同一页面的成立年份、市场、服务能力等公司通用文案只出现一次。
- 技术细节优先使用表格、事实卡、流程图和 `<details>`。
- 每个产品页只突出一个主要决策动作，保留现有 CTA，不新增按钮群。
- 移动端悬浮联系栏不得遮挡 CTA、表单和正文。
- 所有英文经过母语化商务校对，避免模板化、夸张和重复表达。

## 十三、CRM、线索评分与销售执行

### 13.1 CRM 队列

- `standard-equipment`：标准设备和快速价格咨询；
- `engineered-project`：EPC、整套项目和工业终端；
- `partners`：OEM、经销商和区域合作；
- `consumables`：耗材、备件和批发；
- `spam-review`：风险或自动化提交。

### 13.2 高价值线索加分

- 公司域名邮箱；
- EPC、顾问、集成商、终端工厂；
- 上传水质报告或招标文件；
- 明确流量、目标水质、国家和时间；
- 多套设备或整套系统；
- 需要 FAT、国际标准或技术文件；
- OEM/经销商提供年度采购计划；
- 耗材客户提供型号和持续采购频率。

### 13.3 自动动作

- 高分项目立即通知工程销售；
- 普通询盘进入标准销售队列；
- 渠道客户进入合作负责人；
- 超过承诺首响时间自动提醒；
- 24小时无跟进升级提醒；
- 记录 MQL、SQL、报价、赢单、失单原因。

## 十四、数据分析：不改变页面即可完成

接入 GA4 或 Matomo，并统一记录：

- CTA点击；
- 联系页到达；
- 表单开始；
- 表单验证失败；
- 附件上传成功；
- 表单提交成功；
- WhatsApp点击；
- 电话点击；
- 邮箱点击；
- 案例查看；
- documentation展开；
- 产品→询盘路径；
- OEM/经销商/耗材角色选择。

禁止把客户水质、邮箱、电话、消息正文或附件名称发送到通用分析平台。

核心看板：

- 各页面合格询盘率；
- 各国家/渠道的询盘与报价率；
- 表单开始→完成率；
- 附件提交率；
- 首响时间；
- 工程评审率；
- 报价率；
- 赢单金额；
- OEM/经销商的客户终身价值。

## 十五、安全、隐私与合规

- 表单接口限流、幂等、服务器端字段校验。
- 使用 Turnstile/hCaptcha 等低干扰防垃圾机制，异常时才加强验证。
- 管理端和附件使用权限控制与审计日志。
- 隐私政策覆盖表单、附件、CRM、分析和跨境数据处理。
- Cookie/分析策略根据目标市场配置，不默认加载不必要的营销追踪。
- 联系邮箱配置 SPF、DKIM、DMARC。
- 设置 CSP、安全响应头、依赖和错误监控。
- 备份询盘数据库并定期测试恢复。

## 十六、上线与验收计划

### 阶段A：基础设施

- 确定域名和主机。
- 建立单一站点配置。
- 改写所有域名资产。
- 配置 HTTPS、重定向和域名邮箱。

### 阶段B：询盘闭环

- 实现表单 API。
- 实现真实附件上传。
- 接入 CRM 和邮件。
- 加入 WhatsApp/电话但不改现有 CTA 文案。
- 实现来源参数和成功状态。

### 阶段C：信任与客户分流

- 上线首批 3–4 个首页案例和 6–10 个案例详情。
- About 页面加入认证编号、工厂与 FAT 证据。
- 在行业解决方案中加入四个紧凑锚点。
- 在现有联系/备件路径中加入 OEM、经销商、耗材角色。

### 阶段D：AI、分析和质量验证

- 更新 llms、JSON-LD、sitemap、robots 和 `<link rel>`。
- 接入分析事件和 CRM 看板。
- 重跑 GEO 审计四件套。
- 测试桌面、手机、不同浏览器和不同邮箱环境。
- 测试 WhatsApp、电话、上传、CRM、邮件、失败兜底和告警。

## 十七、最终验收清单

### 用户体验

- 现有页面结构和 CTA 文案保持稳定。
- 首页与产品页没有新增大段可见文字。
- 手机端无遮挡、横向滚动或表单难用问题。
- 新行业内容可在30秒内扫描理解。

### 商业闭环

- 表单不依赖本地邮件客户端。
- 附件真实、安全地进入询盘。
- WhatsApp、电话、邮箱和表单都能追踪来源。
- CRM正确分流标准设备、工程项目、渠道和耗材客户。
- 响应承诺可以被系统计时和监督。

### 搜索与AI

- 英文站域名资产全部统一。
- 四件套全部通过。
- AI可通过 llms、sitemap、JSON-LD、`<link rel>` 和折叠文档理解站点。
- 不存在隐藏文本、爬虫专用内容或 cloaking。

### 信任

- 至少6个可核验案例上线，其中3–4个进入首页精选区域。
- 认证、专利和公司信息可核验。
- 项目数字、客户Logo和照片均有证据或授权。

## 十八、预期成熟度

保持现有架构并完成以上整改后：

- GEO/搜索技术成熟度：93–95；
- 人类可读性：维持或提高到86–90；
- 询盘转化系统：91–94；
- 高价值线索识别：90–93；
- 综合网站商业成熟度：90–93。

该方案的重点不是让网站“看起来改了很多”，而是让现有每个页面、CTA和内容模块真正连接到域名资产、信任证据、询盘后端、CRM和销售响应流程。前台保持简洁，后台完成商业闭环。
