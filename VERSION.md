# Konche Water Treatment Technology Website

## 2026-08-26 - Balanced buyer cards and shared landscape CTA banner

- Balanced buyer-route card grids by item count: even totals use equal rows, while odd totals place one additional card in the first row and center the shorter second row on desktop.
- Added alternating coral and deep-green number markers and top rules to the Wholesale Buyers and Service Highlights cards.
- Renamed the Equipment & OEM heading `Customization without a separate catalogue.` to `Customization Supports`.
- Added the supplied mountain-lake photograph as the shared homepage, About, Solutions and product-page closing CTA background, with white and pale blue-gray typography while retaining green primary CTA buttons.

## 2026-08-26 - Small-EPC positioning, solution routes and WhatsApp

- Replaced the homepage Hero with the approved manufacturer, standard/custom/OEM and small-EPC positioning.
- Added all seven existing industry-solution routes to the Project & EPC Support page without duplicating the full solution content.
- Added WhatsApp and Contact Us actions below every module on the two new buyer-route pages.
- Added a shared right-side WhatsApp contact for current website pages using `+86 188 1445 6813`.
- Standardized the supply boundary around documented equipment delivery, technical manuals and remote clarification; removed wording that could imply KONCHE provides destination installation or field service.
- Forced the homepage buyer-card names and descriptions to render in black while preserving the existing card structure.

## 2026-08-26 - Three-path commercial enquiry routing

- Linked the three existing homepage audience cards to dedicated equipment/OEM, spare-parts/wholesale and project/EPC routes while retaining the existing visible CTA language.
- Added compact Equipment & OEM and Project & EPC support pages that reuse current products, factory evidence and project references.
- Extended the existing sourcing page with wholesale-buyer terms and qualification fields instead of creating a duplicate parts page.
- Added role-aware contact routing and synchronized sitemap, `llms.txt` and `llms-full.txt` discovery entries.

## 2026-08-25 - Laboratory ultrapure-water image replacement

- Replaced both the Laboratory Ultrapure Water product image and the Laboratory & Research solution image with the user-approved complete KONCHE equipment photograph.
- Preserved both previous WebP assets with `before-replacement-20260825-62` backup filenames.
- Added a solution-specific contain treatment so the portrait equipment photo remains fully visible inside the 16:9 solution frame.
- Updated image dimensions, alternative text and cache queries; bumped the shared stylesheet cache to `industrial.css?v=20260825-59`.

## 2026-08-25 - Community drinking-water solution image replacement

- Replaced the `Drinking Water Purification & Community Water-Supply Solution` image with the approved stainless-steel pretreatment-vessel version.
- Preserved the previous WebP as `community-drinking-water-before-stainless-replacement-20260825-61.webp` for recovery.
- Updated the image dimensions and alternative text, and bumped the image cache query to `20260825-3`.

## 2026-08-25 - Seawater and brackish-water solution image replacement

- Replaced the `Seawater & Brackish Water Desalination Solution` image with a realistic coastal integrated desalination equipment photograph supplied by the user.
- Preserved the previous WebP as `seawater-brackish-desalination-before-replacement-20260825-60.webp` for recovery.
- Updated the image dimensions, alternative text and caption, and bumped the image cache query to `20260825-3`.

## 2026-08-25 - Project Supply Support navigation and card priority

- Renamed the global navigation label `Spare Parts & Sourcing Service` to `Project Supply Support` while preserving its existing service-page link.
- Moved `Coordinated Equipment Supply` to the first position in the Homepage `PROJECT SUPPLY SUPPORT` card grid.
- Synchronized the same navigation label and card order in the standalone review file.

## 2026-08-25 - Three-audience project positioning alignment

- Standardized the primary positioning around project-engineered water treatment systems for overseas EPC contractors, engineering integrators and industrial clients.
- Rebuilt the homepage Hero message, role cards, technical overview, CTA language and section order around the shared project decision path while keeping all three audiences equally visible.
- Renamed `OUTSOURCING SERVICES` to `PROJECT SUPPLY SUPPORT` and replaced turnkey wording with a defined equipment-supply scope to avoid implying on-site installation.
- Updated About, Contact and Industry Solutions positioning, audience language and quotation CTAs.
- Corrected the company location to Bao'an District, Shenzhen, Guangdong, China and the founding fact to Shenzhen, Guangdong in 1997; added the location to Organization structured data.
- Added a responsive project-scope strip to engineered system pages covering best fit, design inputs, KONCHE scope and client/local-contractor site responsibility.
- Unified shared footer positioning and bumped `industrial.css` to `20260825-58` and `site-enhancements.js` to `20260825-3`.

## 2026-08-25 — Contact Quick Technical Answer accordion

- Converted the complete Contact-page Quick Technical Answer into a default-collapsed accordion.
- Restyled the accordion as a dark industrial panel with white summary, body copy, next-step links and email information.
- Bumped the shared stylesheet cache to `industrial.css?v=20260825-57` across all 24 stylesheet-linked HTML pages.

## 2026-08-25 — Homepage industry copy and Contact module order

- Changed the Homepage Industry Solutions selected-application label and description to black for stronger contrast on the white card.
- Moved the Contact-page `REQUEST A QUOTATION` module above `QUICK TECHNICAL ANSWER`, preserving all existing copy and the subsequent inquiry-form order.
- Bumped the shared stylesheet cache to `industrial.css?v=20260825-56` across all 24 stylesheet-linked HTML pages.

## 2026-08-25 — General consumables documentation accordion

- Converted the long technical scope paragraph under `PRODUCT DOCUMENTATION` on the General Water Treatment Consumables page into a default-collapsed accordion.
- Preserved the complete specifications, replacement cycles and Typical Users content.
- Bumped the shared stylesheet cache to `industrial.css?v=20260825-55` across all 24 stylesheet-linked HTML pages.

## 2026-08-25 — Contact-page readability and English-only audit

- Changed the Contact-page Quick Technical Answer, next-step links and email fact card to high-contrast black text on a light background.
- Changed all Project Inquiry Form labels, entered values, placeholders, helper copy, direct-contact data and side-panel copy to black on white or light surfaces; retained white button labels.
- Audited all public HTML, CSS, JavaScript, JSON and XML content for Chinese characters; translated the remaining HTML and CSS code comments into English.
- Bumped the shared stylesheet cache to `industrial.css?v=20260825-54` across all 24 stylesheet-linked HTML pages.

## 2026-08-25 — Homepage technical portfolio accordion

- Converted the long technical portfolio paragraph beneath the homepage product grid into a default-collapsed accordion.
- Preserved the complete source text while reducing the initial visual length of the Product Portfolio section.
- Bumped the shared stylesheet cache to `industrial.css?v=20260825-53` across all 24 stylesheet-linked HTML pages.

## 2026-08-25 — OBM terminology alignment

- Replaced the remaining public-site `OEM` reference with `OBM`.
- Clarified that KONCHE operates and manufactures under its own brand.

## 2026-08-25 — About Hero workshop image

- Replaced the About-page Hero photograph with the owner-supplied industrial water-treatment workshop image.
- Converted the source PNG to a 90 KB WebP asset while preserving its original aspect ratio and documentary equipment-focused composition.

## 2026-08-25 — About page restructuring and component polish

- Rebuilt `about.html` into the requested B2B trust path: documentary Hero, accordion Quick Technical Answer, Company Introduction, factory workflow, quality control, certifications, team capability, five service promises and a dark page-end inquiry CTA.
- Imported and optimized six owner-supplied factory photographs as local WebP assets; the page uses actual factory equipment and customer-visit scenes instead of showroom or generated marketing imagery.
- Converted the factory and quality-control narratives into responsive horizontal process components that switch to vertical timelines on phones; card grids move from desktop multi-column to tablet two-column and mobile single-column layouts.
- Changed the `OUTSOURCING SERVICES` Explore links to restrained orange-red and normalized all eight UV Service Parts figures to equal media and caption heights without stretching the source images.
- Bumped the shared stylesheet cache to `industrial.css?v=20260825-52` across all 24 stylesheet-linked HTML pages.

## 2026-08-25 — Industry solutions rebuild and dark-surface contrast audit

- Rebuilt `products/industry-solutions.html` as a complete B2B decision path: hero overview, expandable quick technical answer, seven-card portfolio index, seven standardized solution modules and final project CTA.
- Added seven dedicated 16:9 documentary project images. Each image shows two Chinese male engineers checking installed water-treatment equipment in a natural, unfinished industrial site; glossy showroom imagery is intentionally excluded.
- Added per-solution overview, application scenarios, pain points, linked recommended equipment, highlighted treatment outcomes and a project-discussion CTA.
- Corrected the portfolio count from six to seven and added the missing Electronics & Precision Manufacturing solution to the index.
- Added a site-wide deep-navy contrast contract so headings, body copy, labels and links remain light and readable on navy surfaces.
- Optimized generated images to WebP and bumped the shared stylesheet cache to `industrial.css?v=20260825-48` across all 24 stylesheet-linked HTML pages.
- Refined the solution-portfolio introduction by removing its navigation instruction and promoting the solution count to a large green `7` visual anchor.
- Replaced all seven solution images with equipment-only manufacturer-workshop photography. Removed people, installation activity and site-service implications from imagery, alt text and captions; image cache version is `20260825-2`.
- Restyled all seven lower-right `Treatment Outcomes` cards with a light background, black body copy and orange key metrics for stronger readability.
- Marked industry and application keywords across the solutions hero, portfolio tags, overviews and application-scenario lists in orange while preserving black explanatory copy.
- Corrected homepage Product Portfolio, Selected Reference Projects, Where We Deliver and Frequently Asked Questions to use light section backgrounds with black content text.
- Locked the complete site footer to black text on its existing light background.
- Restored the RO complete treatment train to one continuous seven-step line with responsive horizontal scrolling.
- Changed the eight UV Service Parts assets to a four-column desktop grid, two columns on tablet and one column on mobile.
- Removed the `Location` metadata field from all four Reference Project modules that contained it; the fifth module had no Location field.

## Version

- Version name: 框架+产品信息补充
- Snapshot date: 2026-08-11
- Language: English
- Source: Official Chinese website, https://www.konchewater.com/index.html

## Included materials

- `app/`: website source code, homepage, product routes and global styles
- `public/`: public assets and logo files
- `outputs/`: local HTML homepage preview, static product previews and navigation script
- Full product-documentation data and renderer for 17 expanded product/category pages

## Product navigation

Product categories remain the second-level menu. Official product information is translated into English and added as third-level pages:

- Reverse Osmosis System: Single Stage RO, Double Stage RO, Drinking Water Purification and Reclaimed Water Treatment
- Ultra-Pure Water Systems: Industrial Ultrapure Water, EDI Ultrapure Water, Pharmaceutical Purified Water and Deionized Water
- Laboratory Ultrapure Water System: Laboratory Ultrapure Water Equipment
- UF Ultrafiltration System: Hollow-Fiber UF Water Treatment System
- UV Water Sterilizer: TOC UV, Medium-Pressure UV, Low-Pressure UV and Ozone Disinfection
- Spare Parts: General Water Treatment Consumables and UV Disinfection Consumables

The new version includes 8 parent product pages and 16 product-detail pages. The expanded detail pages follow the official Chinese page structure with translated overview, process flow, working principle, component explanation, benefits, technical data, model range, applications, selection guidance where applicable, reference case where applicable, maintenance and FAQ content. Homepage and product-page dropdown links are connected to all available static previews.

## Validation

- TypeScript check passed for the version snapshot.
- ESLint check passed for the updated product navigation and product routes.
- Static link check passed: 19 product preview pages have no missing local targets.
- Detailed-content check passed: all 12 new product pages include the translated documentation structure, including FAQ and maintenance sections.
- Product-section heading update: the eyebrow labels now serve as the only section headings; the former explanatory section titles have been removed from all 12 detailed product pages.
- Single-stage RO correction: the English product pages use desalination `≥99%`, matching the Chinese source page.

## Known items for confirmation

- The official reclaimed-water page uses t/day in its model table and t/h in the hero section; the translated page flags this for project confirmation.
- The official TOC UV page contains inconsistent TOC units in different sections; the version uses the hero-page target of 0.5 μg/L and should be confirmed before publication.
- Official customer names, brand references, certifications, contact details and final legal pages remain subject to the company's publication approval.
- Industry solution images, project case images and client logos remain placeholders.

## Typography and navigation update

- Manrope is now the shared open-source web font for the homepage and all product pages.
- Responsive typography follows the approved hero, section, eyebrow, body, card, navigation and data-number scales.
- Heading line-height is approximately 1.1; body copy uses 1.7-1.8.
- Products now opens a desktop mega menu on hover. Each second-level category reveals only its own third-level products on hover or keyboard focus.
- Existing product-name sizes inside the Products navigation panel are preserved.
- On mobile, the Products panel remains available inside the opened navigation menu and uses the mobile typography scale.
- The Products mega menu was refined into a compact single-column hover panel.
- Secondary and third-level subtitle descriptions are no longer displayed; third-level items use plain text without square code markers.

## Navigation and product layout update — 2026-08-12

- Removed the square letter markers from all second-level product categories.
- Added a gray downward triangle to the five product categories that contain third-level pages: Reverse Osmosis System, Laboratory Ultrapure Water System, UF Ultrafiltration System, UV Water Sterilizer and Spare Parts.
- Promoted Industry Solutions to the first-level navigation immediately after Products.
- Removed product breadcrumbs and the small product-code eyebrow from every static product page.
- Rebuilt complete process flows as connected white arrow blocks and removed the numeric step labels.
- Standardized gray supporting-copy typography across desktop and mobile layouts.
- Center-aligned every product-table header cell.
- Regenerated the homepage and all 19 product previews. TypeScript, ESLint, CSS-brace and static-link checks passed.

## Homepage refinement — 2026-08-12

- Removed Projects from the main navigation while retaining Industry Solutions as the first-level item after Products.
- Preserved the Our Projects content section and its project carousel on the homepage.
- Updated the hero description to include advanced RO systems, ultrapure water systems, ultrafiltration systems, UV disinfection systems and ozone disinfection systems.
- Removed the WATER, ENGINEERED BETTER hero eyebrow.
- Removed the 01–06 service numbers and the 01–04 Industry Solutions numbers, with the remaining content realigned for desktop and mobile.
- Regenerated all static previews. TypeScript, ESLint and static-link checks passed.

## Primary navigation restructuring — 2026-08-14

- Replaced the former Products hierarchy with ten first-level navigation items in this order: Home, Reverse Osmosis System, Laboratory Ultrapure Water System, Containerized Water Treatment System, Ultrafiltration System, UV Water Sterilizer, Water Treatment Consumables, Solutions, Resource and About Us.
- Promoted the six former second-level product categories to the first level.
- Converted their former third-level product pages into second-level hover menus under the corresponding first-level category.
- Added a two-row desktop header so the long product names remain readable, with an automatic compact menu below 1180 px.
- Added mobile submenu controls for each category that contains child products.
- Connected Solutions to the existing Industry Solutions page, Resource to the homepage insights section and About Us to the homepage company section.
- Updated the shared source components, homepage preview, all 19 product HTML previews and the local navigation script.
- TypeScript validation passed. The full hosting build remains blocked by the version snapshot's pre-existing missing `worker/index.ts` entry.

## Solutions page expansion — 2026-08-14

- Rewrote the Solutions hero introduction in English for overseas EPC contractors, engineering companies, factory buyers and project owners in the Middle East, Africa and Southeast Asia.
- Added six complete B2B solution sections: industrial pure and ultrapure water, seawater and brackish water desalination, centralized drinking-water and community supply, food and beverage water treatment, industrial UF pretreatment and laboratory water treatment.
- Every solution uses the same five-module structure: Overview, Application Scenarios, Main Pain Points, Recommended Equipment and Treatment Outcomes.
- Connected all recommended equipment items to the corresponding KONCHE product pages.
- Excluded household and residential water-purification positioning throughout the page.
- Added responsive desktop and mobile layouts, a six-item solution index and concise SEO-oriented industrial copy.
- Updated both the shared website source and the local static Solutions preview.
- TypeScript validation passed; all 20 static HTML pages retain valid local links. The full hosting build is still blocked by the snapshot's pre-existing missing `worker/index.ts` entry.

## Navigation TDK and company pages — 2026-08-14

- Added the approved English Title, Description and Keywords metadata to eight relevant product/solution pages.
- Applied the Electronic-Grade Ultrapure Water TDK to the existing Double Stage RO page pending a dedicated electronic-grade ultrapure-water page.
- Created standalone About KONCHE and Contact KONCHE pages so each can carry its own page-level TDK.
- Added Contact Us as the final main-navigation item on the homepage, all product pages, About and Contact.
- Linked the header quotation CTA to the new Contact page.
- Preserved the existing settings for Home, Containerized Water Treatment System and Resource because no new TDK was provided for those pages.
- Updated the source routes and the local static review package, which now contains 22 HTML pages.
- TypeScript validation passed and all 22 static pages have valid local links. The full hosting build remains blocked by the snapshot's pre-existing missing `worker/index.ts` entry.

## Ultra-Pure Water Systems category — 2026-08-14

- Added Ultra-Pure Water Systems as a first-level navigation item immediately after Reverse Osmosis System on every page.
- Created a standalone Electronic-Grade Ultrapure Water Systems source route and local static preview.
- Moved the approved electronic-grade ultrapure-water TDK from Double Stage RO to the new category page to prevent conflicting search intent.
- Restored product-specific metadata to the Double Stage RO page.
- Added an initial B2B content framework covering process architecture, industrial applications, engineering inputs and project support without inventing unverified performance parameters.
- The local review package now contains 23 HTML pages, with no missing local links.
- TypeScript validation passed. The full hosting build remains blocked by the snapshot's pre-existing missing `worker/index.ts` entry.

## Ultra-Pure Water content expansion — 2026-08-14

- Expanded the Ultra-Pure Water Systems parent page into a complete category overview covering industrial ultrapure water, EDI, pharmaceutical purified water and mixed-bed deionized water.
- Added four second-level product pages in the same order as the navigation: Industrial Ultrapure Water System, EDI Ultrapure Water System, Pharmaceutical Purified Water System and Deionized Water System.
- Translated and reorganized the four current official Chinese product pages into the established English page structure: overview, complete process flow, working principle, benefits, technical data, model range, applications, selection guidance, reference case where available, FAQ and maintenance.
- Used the supplied RTF to broaden the industry applications and engineering-input sections, while avoiding unsupported claims and treating conflicting values as confirmation items.
- Added page-level English TDK for all four new product pages and connected the new dropdown links across the homepage, all product pages, About and Contact.
- The local static version now contains 27 HTML pages; all local links resolve, all five Ultra-Pure Water pages contain English-only content, and TypeScript validation passes.
- The production build reaches the existing hosting configuration and remains blocked by the pre-existing missing `worker/index.ts` entry; this is unrelated to the new product content.

## GEO and AI-search readiness — 2026-08-14

- Reviewed all five local third-party GEO repositories and recorded their applicable tools, limitations and implementation decisions in `GEO-TOOL-REVIEW.md`.
- Added unique titles, descriptions, canonical URLs, robots directives, social metadata and JSON-LD entity graphs to all 27 static pages.
- Added direct-answer technical sections to the homepage and all 24 product/solution pages, including best-fit guidance, required engineering inputs and explicit project-guarantee boundaries.
- Converted detailed product-documentation labels into semantic H2 headings while preserving the existing visual treatment.
- Added `robots.txt`, a 27-URL `sitemap.xml`, validated `llms.txt`, detailed `llms-full.txt` and a publication fact-check file.
- Added a site-specific 1200×630 Open Graph card and accessibility skip links.
- Added reproducible `tools/geo_enhance.py` and `tools/validate_geo.py` utilities.
- Final local validation passes for all 27 pages, JSON-LD blocks, canonical URLs, sitemap coverage, local links, duplicate IDs and CSS brace balance.

## Final delivery hardening — 2026-08-15

Scope: apply the remaining third-party GEO repository guidance, close the applicable items from the online audit issue list (`outputs/.../Konche123网站问题整改清单`), unify the visual system, and make every check reproducible on the delivery machine (Node; no Python available locally).

### GEO additions

- Homepage now carries a visible, expandable six-question FAQ whose text is generated from the same source strings as its FAQPage JSON-LD, keeping schema and visible content exactly in sync (audit KON-007 pattern).
- Every page's JSON-LD graph gained Organization telephone and an inline sales ContactPoint; the contact page adds a standalone ContactPoint node (audit KON-019 complete).
- Pages with a direct-answer block declare `speakable` (SpeakableSpecification on `.geo-answer-copy`).
- The solutions page adds a Service node; the 28 sitemap URLs remain one-to-one with canonical pages, now including privacy.html.
- All 16 product detail pages gained enriched answer-first paragraphs (definition pattern, page-documented reference values, pronoun-light), an "On this page" sticky section TOC with heading anchors, and a visible "Related systems" card section mirrored by Product.isRelatedTo (pillar-cluster internal linking).
- `robots.txt` regenerated with the extended AI-crawler policy: GPTBot, OAI-SearchBot, ChatGPT-User, ClaudeBot, anthropic-ai, PerplexityBot, Google-Extended, GoogleOther, Applebot-Extended, Amazonbot, FacebookBot and CCBot allowed; Bytespider disallowed (documented decision).
- `llms.txt` regenerated with Key facts and Contact sections plus the privacy page; `sitemap.xml` lastmod and every page's dateModified updated to 2026-08-15 together (all pages substantively changed in this pass).
- No AggregateRating, no SearchAction, no fabricated sameAs or authors remain anywhere (audit KON-005/KON-006 stay closed).

### New pages and repaired links

- `privacy.html`: standalone privacy policy (no cookies/trackers on this static site) with full TDK and page schema; in the sitemap.
- `404.html`: noindex, sitemap-excluded, with links to every current section and guidance for moved article URLs.
- Footer Privacy Policy and Sitemap links across all pages now point to real targets instead of `#top`/index placeholders.

### Visual and interface

- Typography unified on the documented Manrope system; the late Georgia/Trebuchet override layers were removed.
- Homepage: "OUR CLIENTS" logo-placeholder wall replaced by an "INDUSTRIES SERVED" badge grid built from claims already supported on the site; the "1,000+ customers" figure was removed pending evidence (see GEO-FACT-CHECK.md); the project carousel placeholder became a CSS blueprint process illustration; the solutions panel placeholder text became a schematic illustration; the single insight card became a three-card guidance grid linking to real in-site destinations.
- Product pages: sticky TOC sidebar (chip row below 1020px), related-systems cards, table row hover polish, scroll-margin for anchored sections.
- Fixed a mobile (≤390px) horizontal overflow on product pages caused by the arrow process-flow layer; verified no overflow across sampled pages at 390px and no desktop regression at 1440px.
- Browser-verified at 1440px and 390px: homepage sections, product documentation TOC, related cards, FAQ accordion, 404 and privacy pages.

### Toolchain (Node, dependency-free)

- `tools/validate_geo.mjs`: full gate incl. FAQ-schema-visible parity, no-AggregateRating/SearchAction, viewport, title/description advisories, sitemap↔canonical set, lastmod↔dateModified sync, llms.txt structure, robots.txt crawler policy. Passing: 28 pages, 28 URLs.
- `tools/geo_audit.mjs`: content-quality and citability heuristics ported from `claude-seo-main/scripts/content_quality.py` and `geo-seo-claude-main/scripts/citability_scorer.py` (heading-block semantics, filler/AI-phrase lists, information density, best-passage scoring). Result: 0 filler, 0 AI-pattern, 0 low-density pages; product best-passage scores 35–58/100.
- `tools/deploy_check.mjs`: post-deploy live verification (llms content types, 28-URL all-200 sitemap, sample-page canonical/OG/JSON-LD, old-/news/ handling, host convergence) — smoke-tested 11/11 against a local server.
- `DEPLOYMENT.md`: nginx configuration for the www/non-www 301 without double slashes (KON-003), exact-match text/plain locations for llms files (KON-004), old-article 301 map (KON-001), release checklist (KON-008) and search-platform submission steps (KON-021).
- Python tools remain for reference; the Node suite is authoritative for this package.

### Validation summary

- `node tools/validate_geo.mjs` — passed (28 pages, 28 sitemap URLs, FAQ schema visible, links OK, CSS balanced).
- `node tools/geo_audit.mjs` — no filler/AI-pattern/low-density flags.
- `node tools/deploy_check.mjs http://localhost:8642` — 11/11.
- Browser QA at 1440px and 390px — no layout overflow; TOC anchors verified by click.

## Citability and comparison-content iteration — 2026-08-15 (pass 3)

A third deep-read of the third-party repositories (citability rubric, comparison-builder method, agent-friendly checklist) produced this pass, implemented by `tools/geo_enhance_v3.mjs` and locked in by new `validate_geo.mjs` invariants.

### Answer-block architecture (all 26 answer pages)

- Every QUICK TECHNICAL ANSWER block now follows one architecture: citable answer passage → visible uppercase scope sub-heading (h3, green-dash style) → page-unique boundary paragraph. The previous identical ~100-word boilerplate paragraph that repeated verbatim on every page was removed entirely (cross-page uniqueness is now a validator invariant).
- The 11 weakest pages (index, RO, UV, UF, spare parts, laboratory, containerized, electronic-grade, solutions, ozone, pharmaceutical) received new 134–167-word answer passages in the citability sweet spot: definition-first, pronoun-light, proper-noun dense, carrying only reference values already documented on the page or its linked cluster pages (GEO-FACT-CHECK discipline; no new claims).
- `about.html` gained a "Who is KONCHE?" brand fact-card answer block (legal entity, 1997, Jiangsu, audience, regions, portfolio) with speakable on its AboutPage node — the brand-verification intent page for AI engines.
- Content-audit result: weak best-passage pages 10 → 0; rewritten pages now score 39–62/100 (were 29–35).

### Same-caliber technology comparison tables

- Three category pages gained comparison tables built to the comparison-builder rules (identical dimensions for every option, own limitations included, conditional decision guidance, verification footer): Single Stage vs Double Stage RO (7 dimensions), Low-pressure UV vs Medium-pressure UV vs TOC UV vs Ozone (6 dimensions, 4 options), UF vs RO membrane roles (7 dimensions).
- Table header cells link to the corresponding product pages (pillar-cluster contextual anchors); `scope="col"` on every header for accessibility; tables reuse the existing `detail-table` visual system and scroll horizontally inside their wrapper on mobile.

### Toolchain and validation

- `tools/geo_enhance_v3.mjs`: reproducible, idempotent (fresh-run and refresh modes) generator for the answer architecture and comparison sections, with word-count and definition-pattern guards.
- `tools/validate_geo.mjs` new invariants: geo-answer scope h3 present; answer-passage word count 120–185 on the 12 rewritten pages; boundary-paragraph first-60-chars unique across pages; comparison sections on the three category pages with ≥6 rows, decision guidance, a limitations row and column scopes.
- Verified brand-definition consistency: Organization JSON-LD description and llms.txt description are verbatim identical (meta/OG descriptions intentionally remain search-optimized variants).
- Agent-friendly static checks: no div/span onclick handlers, no unlabeled inputs (site is mailto-based, no forms).
- Browser QA at 1440px and 390px: new h3 sub-headings, comparison tables (including the 5-column UV table) and about answer block render correctly; comparison tables scroll inside their container on mobile; no page-level horizontal overflow.

## Self-audit hardening pass — 2026-08-15 (pass 4)

A new `tools/site_audit.mjs` (static quality dimensions not covered by the GEO validator: title/description lengths, image CLS attributes and alt rules, font-display, in-body internal-link floors, DOM-size guideline, heading-order sanity, og:image dimensions, html lang, llms-file link integrity) drove a fourth fix round.

- Fixed every audit finding: 404 title lengthened; about/contact meta descriptions brought into the 110–160 window; the electronic-grade family table's four rows now link to their child product pages (pillar→children contextual links, lifting in-body links from 2 to 6 and verified as real links in the accessibility tree); llms-full.txt gained a "Direct answers and comparison guides" section documenting the pass-3 assets with anchor URLs; llms.txt descriptions now mention the comparison guides, fact card and quotation process.
- New GEO surfaces: contact.html gained a "How do I request a KONCHE quotation?" answer block (definition-first, 151 words, RFQ inputs and response flow, speakable) — the conversion page now serves "how to contact/quote KONCHE" intent.
- Tail-page citability lift: the seven product pages whose answer passages sat below 50/100 (deionized, general consumables, UV consumables, drinking water, double-stage RO, low-pressure UV, plus the EDI/TOC-UV definitional-verb recognition) were extended into the 117–151-word window using only page-documented values. Audit scores: deionized 40→56, double-stage 44→62, drinking 44→54, general consumables 37→49, UV consumables 37→48, low-pressure UV 43→56, EDI 43→49, TOC UV 40→46.
- `geo_audit.mjs` definition-pattern heuristic extended to recognize "An X system combines/uses/applies/treats…" definitional-verb openings (measurement fidelity fix, not a threshold change).
- `site_audit.mjs` runs clean: 0 errors, 0 warnings, 0 notes across 29 pages.
- Browser QA: contact answer block and the linkified family table verified at 1440px; deploy_check 11/11.

## Industrial engineering redesign + product photography — 2026-08-18 (pass 5)

Applied the approved industrial palette across all 29 pages and integrated the supplied product photography (folder `Konche-Website-All-Images-2026-08-18`, 27 images) as real `<img>` assets. Implemented by `tools/industrial_refresh_v1.mjs` (idempotent, marker-based revert) plus the new `app/industrial.css` loaded after globals.css on every page.

- Palette: deep navy #071C2C (header, footer, dark sections), graphite #252D31 (engineering nameplate, module headings), silver #D8DEE0 (borders, tables, zebra rows), engineering white #F5F7F6 (page background), safety orange #F47A22 (primary buttons, equipment codes, key parameter values, active states), status green #3F9142 (status chips only). The legacy green accent is fully retired; theme-color meta updated to #071C2C. The dark logo is rendered as a white monochrome mark on navy surfaces via CSS filter.
- Product hero upgrade (24 pages): left column gains a 4-cell core-parameter strip (orange tabular values, all values verbatim from each page's documented text; e.g. double-stage RO shows ≤2 μS/cm · ≥99% · 0.25–500 m³/h · ≥75%); 16 pages swap the CSS graphic for a real equipment photo (studio white-background shots use contain + multiply blend into the page; scene photos fill cover); a vertical equipment code (e.g. RO-S / 02) runs along the photo edge; single-stage RO carries three static hairline component callouts (membranes / pump / PLC cabinet).
- Graphite engineering nameplate bar under every product hero: KONCHE plate brand, model line (e.g. MODEL KONCHE RO-D · DOUBLE STAGE REVERSE OSMOSIS), three status chips (documented capabilities only) and an engineering-plate serial line.
- Galleries: general consumables (9 photos), UV consumables (5), spare-parts overview (4), containerized installation reference (1); homepage hero uses the installed containerized-plant photo and the four industry-solution tabs now swap real equipment photos via the tab switcher.
- Images stored in `app/images/products|consumables/`; originals >400KB recompressed to WebP (q82, max 1600px) — hero photos now 75–170KB each. All imgs carry width/height (CLS) and 10–125-char alt text per site_audit rules; hero eager + fetchpriority, galleries lazy.
- Fixed a pre-existing bug: the LOCAL PREVIEW marker rendered as unstyled text on 28 pages; now a uniform fixed badge site-wide.
- Top navigation unchanged: all 12 menu items and the Request a Quote CTA retained; CTA restyled safety orange.
- Validation: validate_geo 28 pages/28 URLs pass; geo_audit 0 filler / 0 AI-pattern / 0 low-density / 0 weak passage; site_audit 0 errors; deploy_check 11/11 on the local server. Browser QA at 1440px and 390px: navy header/footer, photos loaded, nameplate wrap, vertical-code reflow, hamburger menu open, zero horizontal overflow on all checked pages.
- Backup before the sweep: `.codex_artifacts/backup-before-industrial-2026-08-18.tar.gz` (29 HTML + CSS + config). sitemap lastmod and all dateModified values synchronized to 2026-08-18.

## P&ID process diagrams + specification table upgrades — 2026-08-18 (pass 6)

Implemented by `tools/pid_spec_v1.mjs` (idempotent; step data embedded in the script) plus new styles in `app/industrial.css` §11.

- Process flow upgrade (15 pages that document a process train): the plain chevron text steps are replaced by an inline simplified P&ID — SVG equipment symbols mapped by keyword (feed/product tanks, pumps, RO/UF membrane capsules, UV chambers, EDI stacks, mixed-bed polishers, media filters, ozone generator, venturi mixer, instrument circles with CT/UI/TI/OI tags, point-of-use endpoints), stage tags (TK-01, P-01, RO-01…), an arrowed main line, orange highlight on each page's core stage type, dashed outlines for optional stages, and a visible "PROCESS TRAIN" caption. All step text remains in the DOM (SVG text + caption), so no GEO content was lost. Wide diagrams (up to 10 stages) scroll horizontally inside their column on desktop and mobile; grid columns gained min-width:0 guards so the page itself never overflows.
- Specification tables (13 pages, "Parameter | Specification/Reference" tables): split into Parameter | Value | Unit with the trailing unit extracted into its own column (m³/h, μS/cm, MΩ·cm, °C, %, etc.); rows whose values match the page's headline parameter strip get an orange key-row highlight (orange left rule + tint + bold label) with a ◆ legend; all table headers now carry a 2px safety-orange top rule. Comparison tables on the three category pages were deliberately left structurally untouched (validator invariants).
- Validation after the pass: validate_geo 28/28 pass (the SVG carries no <title> element precisely to keep the one-<title>-per-page invariant), geo_audit all zeros, site_audit no errors, deploy_check 11/11; browser QA at 1440px/390px confirmed diagram rendering, internal scrolling and zero page overflow.

- Logo correction (same day): the blue-green original brand mark (blue ring + green drop) is now rendered in its original colors everywhere; the earlier white-mono treatment was removed. On the navy header and footer the emblem sits on a white nameplate chip (silver hairline border, 4px radius) so the brand colors stay visible — header (desktop 132px bar and 76px mobile bar) and footer verified at 1440px/390px with no overflow.

## Homepage banner replacement — 2026-08-18 (pass 7)

- Replaced the homepage hero photo (containerized installation) with the supplied ultrapure-water banner design: the equipment region was cropped from the approved artwork (the design's baked-in left text — including the unverifiable "30 years" claim — was deliberately excluded), recompressed to `app/images/products/hero-ultrapure-banner.webp` (82KB, 811×931), and rendered as a full-bleed hero background with a seamless white→light-blue scrim (edge color sampled from the artwork).
- Left copy now carries the requested three-point statement 品质严控 · 非标定制 · 稳定运行 as orange-numbered stencil items between hairlines; the English H1, description, CTAs and trust line are retained (GEO text unchanged). The old right-column photo figure and its labels were removed; hero is single-column over the banner.
- Mobile: statement stacks vertically, banner scrim strengthens for legibility; verified 1440px/390px with zero overflow. All four validators green (validate_geo 28/28, geo_audit zeros, site_audit clean, deploy_check 11/11).
- Banner statement wording switched to English per owner review: 01 Strict Quality Control · 02 Custom Engineering · 03 Stable Operation (typography tuned for Latin text; verified 1440px/390px, validators green).
- Fixed a strip-regex defect that left two orphaned Chinese statement items (02/03) on the homepage after the English switch; the points-strip now removes the full container and a defensive orphan sweep runs on every script pass. Homepage hero verified Chinese-free with DOM structure restored (description/CTAs back inside hero-content); validators green.
- Header reverted to the engineering-white translucent bar (owner review, referencing the pre-redesign layout): original blue-green logo sits directly on the light surface with no chip, wordmark in navy, nav links graphite with orange hover, mobile menu/dropdown on white. theme-color meta now #F5F7F6 on all 29 pages; footer remains navy with the white logo plate. Verified 1440px/390px (menu open/close, no overflow); validators green.
- Footer switched to light silver (#EDF1F2) with a 2px safety-orange top rule: original blue-green logo shown directly (plate removed), navy brand wordmark, graphite body text, orange hovers — matching the light header. Verified computed styles and no overflow at 1440px; validators green.

## RO page revision + site-wide hero cleanup — 2026-08-19 (pass 8)

Owner requirements from "reverse osmosis system 页面修改要求.docx"; backup in `_backup_before_ro_page_revision_2026-08-19/` (28 pages + industrial.css/globals.css).

- Breadcrumb restored to the visible document flow (was `position:absolute; top:124px`, covered by the centered hero copy): it now spans the hero grid row above the H1 with one line of spacing (`industrial.css` §18), desktop and mobile. Breadcrumbs were also added to the 22 pages that lacked them (16 sub-product pages with parent-category segment, electronic-grade and industry-solutions category pages, about/contact/privacy/404), so all 28 pages now show exactly one visible breadcrumb.
- Meaningless hero code labels removed site-wide per owner decision (no GEO value): `product-code` chips ("RO / KONCHE SYSTEMS" style, 11 pages) and `ind-vertical-code` vertical codes ("RO-S / 02" style, 16 pages).
- RO hero right visual: decorative orbits/watermark replaced with the supplied double-stage RO equipment photo (`app/images/products/ro-double-stage-system.png`, 1032×1382) in a bordered photo frame with cover crop (500px desktop / 340px mobile).
- PRODUCT CAPABILITY: new slogan line "Stable, reliable, high-quality purified water — with lower energy consumption and operating costs." under the section H2; card 03 heading changed to "Flexible configuration to match your water source." (body text unchanged — it already carried the requested options sentence).
- Terminology unified to "Double Stage RO" (owner note: mixed "double stage"/"second pass" wording suggested two different systems): RO page hero subtitle, capability card 02, GEO answer, comparison table ("across both stages", "favor a Double Stage RO"), CTA and all three meta descriptions; double-stage-ro.html and single-stage-ro.html body copy aligned ("second membrane stage", "single-stage operation/desalination"); fixed an "osmososis" alt-text typo found in the pass. Site-wide footer SEO keyword lines deliberately keep both search synonyms.
- New PROCESS FLOW section on the RO page (after TECHNOLOGY COMPARISON): supplied flow diagram (`app/images/products/ro-process-flow-diagram.png`, 1408×768) in a captioned orange-rule panel + intro paragraph (pretreatment protects membrane life; EDI can upgrade to ultrapure) + "Key process stages" grid of 5 cards (pretreatment, security filter, high-pressure pump, RO membrane elements incl. genuine-brand statement, pure water tank & post-treatment).
- Freshness metadata: `dateModified` bumped to 2026-08-19 on all 27 changed pages and sitemap `<lastmod>` for the 27 non-homepage URLs; RO page visible "updated" lines moved to 19 August 2026.
- Validation: site_audit clean for live pages (flow-diagram alt shortened to 123 chars, under the 125 limit); geo_audit unchanged from previous pass (only the pre-existing privacy.html weak-passage flag); validate_geo green for all 28 real pages — remaining validator output only flags files inside `_backup_*` folders (tool scans them; same noise existed before this pass). deploy_check failures are live-site only (remote canonical/robots/sitemap states), unaffected by local edits. Browser QA: RO page desktop + 390px mobile, single-stage-ro and about.html spot checks — breadcrumb visible, no overlap, no overflow.
- Owner follow-up (same day): "Key process stages" is part of the PROCESS FLOW module — the orange separator rule on the sub-heading was removed and the 5 stage cards were condensed into a five-line list (bold stage name + one sentence each, small orange tick markers), cutting the module height roughly by two thirds. Verified at 1280px.

## Single Stage RO page revision — 2026-08-19 (pass 9)

Owner requirements from "Single Stage Reverse Osmosis System.docx"; backup in `_backup_before_single_stage_revision_2026-08-19/` (page + industrial.css).

- GEO quick-answer first paragraph now closes with the bolded statement "It delivers reliable water quality at low energy and operational cost for a wide range of industrial applications."
- The application list "boiler make-up, electroplating rinse water, food ingredients and chemical processes" is bold and orange (#F47A22, new `.hl-orange` utility) in both places the sentence appears — the hero paragraph and the PRODUCT DOCUMENTATION intro.
- COMPLETE PROCESS FLOW module: the generated P&ID line diagram (pid-scroll/pid-svg) was replaced by the supplied full-colour flow diagram `app/images/products/single-stage-ro-process-flow.svg` (1200×650, English labels, rendered full-width inside the existing pid-figure panel); the crawlable "PROCESS TRAIN" figcaption is retained. Verified at 1280px.
- Owner follow-up (same day): the hero-paragraph occurrence of the application list was reverted to plain text per owner request; the orange highlight remains only in the PRODUCT DOCUMENTATION intro.
- Owner follow-up (same day, colour decision): hero core-parameter strip values (`.ind-params dd` — ≥99%, ≤10 μS/cm, capacity, recovery …) switched from safety orange #F47A22 to the status green #3F9142 on all product pages via the single industrial.css rule; verified on single-stage-ro.html.

## Site-wide cleanup — 2026-08-19 (pass 10)

Pre-cleanup full snapshot: `_site_backups/pre_cleanup_2026-08-19/` (outside the site root, alongside the 8 moved legacy backup folders in `_site_backups/legacy_from_site_root/`).

- CSS dead-code removal: ~70 verified-unused classes had their rules deleted from globals.css and industrial.css — the retired homepage blueprint schematic (tank/pipe/membrane/pb-*/plant-visual/water-glow/floor-line/label-top/label-bottom), the removed homepage projects/logo-wall section (project-*, logo-*), the pre-P&ID flow steps (detail-flow-step*), the deleted hero labels (product-code, ind-vertical-code), the orphaned homepage statement strip (ind-banner-points), and assorted leftovers (service-icon, industry-row, about-focus, button-outline, hero-visual, visual-label, light-link, industry-tab-number, ind-gallery-title, solution-image-* variants). Multi-selector rules kept their live selectors.
- Legacy green palette stripped from globals.css where industrial.css sets the same property on the same selector (#479544 / #6da36f / #7ea87e / #6c8f78 / #9fbca0 / #b7d5b6 / #c6d7c7 / #164937 and their rgba() tints; !important declarations untouched). Result: globals.css 83.1KB → 62.0KB (−25%, 183 rules removed), industrial.css 40.8KB → 31.3KB (−23%, 34 rules removed); both brace-balanced.
- 8 in-root `_backup_before_*` folders (6.6MB) moved out of the site root to `_site_backups/legacy_from_site_root/` — deploy packages and validators no longer see them.
- `tools/DO-NOT-RUN-README.txt` added: validators are safe; the one-time transform scripts (geo_enhance*, industrial_refresh_v1, pid_spec_v1) must not be re-run because they would clobber manual revisions (e.g. regenerate the P&ID over the supplied SVG on single-stage-ro.html).
- Post-cleanup validation: validate_geo **exit 0 — 28 pages, 28 sitemap URLs, valid JSON-LD, FAQ parity, links OK** (first fully green run; previous noise came from scanned backup folders); geo_audit 29 pages, no flags; site_audit only pre-existing homepage client-logo notes. Browser regression on RO page, single-stage-ro, homepage and about: breadcrumbs, hero photo, green parameter values, slogan, process-flow SVG, orange highlight, nav/CTA all intact. CSS-defined class count dropped 420 → 334; remaining "unused" scan hits are the JS-injected inquiry-form and social-dock classes (alive).

## Double Stage RO page revision — 2026-08-19 (pass 11)

Owner requirements from "双级反渗透修订.docx"; backup at `_site_backups/backup_before_double_stage_revision_2026-08-19/`.

- COMPLETE PROCESS FLOW: the generated P&ID line diagram and its "PROCESS TRAIN" caption were replaced by the supplied full-colour flow diagram `app/images/products/double-stage-ro-process-flow.png` (1600×720, English labels, optional-UV and concentrate-return shown). The old description paragraph was replaced by the owner text (translated, same wording as the RO category page): pretreatment as the first line of membrane protection + EDI upgrade path to ultrapure water.
- Removed the "◆ Key parameter — highlighted rows match this page's headline specification." legend line.
- MODEL RANGE replaced with the owner five-model table (KC-RO-0.25/0.5 … KC-RO-100+, capacities 0.25–0.5 to 100+ m³/h, application per tier) plus a customization note (t/h rendered as m³/h per site convention).
- REFERENCE PROJECT replaced with the delivered 20 m³/h Huizhou optoelectronics double-stage RO case (anonymized intro line, Location/Industry/Capacity/Process meta, ≤2 μS/cm permeate feeding downstream EDI, ~15% line energy reduction).
- MAINTENANCE replaced with the owner four-item schedule (cartridge filters 1–3 months, membrane cleaning 6–12 months, activated carbon annually, weekly conductivity/pressure logging).
- FAQ module deleted entirely per owner decision — visible article, TOC link, FAQPage JSON-LD node and the Product node's dangling `subjectOf` #faq reference all removed together, keeping validate_geo FAQ parity green.
- Verification: validate_geo exit 0, site_audit clean for this page; browser DOM checks all green (diagram visible, 5 model rows + note, Huizhou case, 4 maintenance rows, FAQ gone).
- Owner follow-up (same day): RELATED SYSTEMS cards on double-stage-ro.html replaced with the owner's five systems — Ultra-Pure Water System, Ultrafiltration System, UV Water Sterilizer, Drinking Water Purification System, Water Treatment Consumables — each linked to its category/product page (link targets verified); the previous Single Stage / Industrial Ultrapure / EDI cards were removed.

## Drinking Water Purification System page revision — 2026-08-19 (pass 12)

Owner requirements from "drinking water purification system 修订意见.docx"; backup at `_site_backups/backup_before_drinking_water_revision_2026-08-19/`.

- GEO quick answer fully rewritten in the owner's layered structure: "In short" statement, KONCHE process+capacity paragraph, Typical projects line, "What it delivers" bullet list, "How we scope every project" (3 inputs + engineering-review closing), and a "Where we stand out" two-column table (regulatory compliance / microbiological safety / hygienic operation / no guesswork). Facts column updated; new `.geo-answer-copy ul/li` dash-list styling added to industrial.css.
- CORE BENEFITS replaced by the official-site selling points (GB 5749 direct-drinking output, ≥99.99% sterilization via UV+terminal UF, multi-temperature dispensing, on-site purification vs barreled water, smart management with maintenance alerts).
- MODEL RANGE replaced by the official-site specification table: KCDW-1/5/20/50 with a dedicated Capacity column (t/h rendered as m³/h) per-tier applications, plus the customization note.
- APPLICATIONS replaced by the official-site industry scenarios as a 3-column table (Schools / Residential communities / Office buildings / Public venues / Factories with water point and system value).
- FAQ extended with the three owner questions (direct drinking vs GB 5749, advantages over barreled/bottled water incl. 30–50% cost line, no dedicated operator needed) — added to both the visible module and the FAQPage JSON-LD mainEntity.
- COMPLETE PROCESS FLOW diagram replaced by the supplied `app/images/products/drinking-water-process-flow.png` (1600×823); the "PROCESS TRAIN — City water → Pretreatment → RO desalination → Post activated carbon → UV disinfection → Terminal UF → Drinking-water point of use" caption was kept verbatim per requirement.
- Verification: validate_geo exit 0 (FAQ schema/visible parity holds), site_audit clean for this page; browser DOM checks green (layered GEO with table, 5 benefit rows, capacity column, 3-col applications, 7 FAQ items, diagram + caption).
- Owner follow-up (same day): the sentence below the drinking-water process-flow diagram ("The combined process reduces dissolved salts… confirmed by the applicable project standard and testing plan.") was removed per owner request; the PROCESS TRAIN caption stays.
- Owner follow-up (same day): drinking-water-system.html RELATED SYSTEMS replaced with the owner's six items — Double Stage RO System, Disinfection & Sterilization Equipment (UV category), Laboratory Ultrapure Water Equipment, Containerized/Integrated Water Treatment System, Filter Cartridges (general consumables) and Water Treatment Consumables & Parts — all six link targets verified.
- Owner follow-up (same day): reclaimed-water-treatment-system.html RELATED SYSTEMS replaced with the owner's four items — Reverse Osmosis Equipment, UF Ultrafiltration System, Double Stage RO System and Water Treatment Consumables & Parts (link targets verified).
- Owner follow-up (same day): reclaimed-water-treatment-system.html COMPLETE PROCESS FLOW diagram replaced by the supplied `app/images/products/reclaimed-water-process-flow.svg` (1600×720, English labels); the existing PROCESS TRAIN caption was kept (alt trimmed to the 125-char audit limit). Backup: `_site_backups/backup_before_reclaimed_flow_svg_2026-08-19/`.
- Owner follow-up (same day): the reclaimed-water flow diagram asset was updated to the owner's revised "中水回用 (2).svg" (two-row layout, 1600×950); the img dimensions were synced to the new aspect ratio. Verified rendering and validate_geo green.
- Owner follow-up (same day): all text inside the reclaimed-water flow SVG enlarged by two size steps (11.5→15, 12→16, 13→17, 14→18, title 32→40) by editing the SVG font-size attributes directly; zoom-in browser check confirmed no label overflow or overlap. Pre-change asset kept at `_site_backups/reclaimed-flow-svg-before-font-bump.svg`.
- Owner follow-up (same day): reclaimed-water-treatment-system.html hero right visual switched from the legacy decorative graphic (orbits + "RW" watermark) to the supplied equipment photo `app/images/products/reclaimed-water-treatment-system.png` (1024×682) in the standard photo frame. Verified rendering and validators green.

## Round 1 — ultrapure-water family alignment with CN official site — 2026-08-19 (pass 13)

Five pages aligned to their konchewater.com CN product pages (owner rule: CN site is the source of truth for product parameters). Backup: `_site_backups/backup_before_upw_family_round1_2026-08-19/`.

- industrial-ultrapure-water-system: GEO quick answer rewritten in the layered 3-question skeleton (In short / facts / Typical projects / What it delivers / Why trust KONCHE / Where-we-stand-out table); CORE BENEFITS = CN selling points (stable 18.2 MΩ·cm, no acid/caustic regeneration, full-auto PLC, modular 0.5–100 m³/h, direct consumables supply); MODEL RANGE = KC-UPW-0.5…100; INDUSTRY APPLICATIONS = six CN scenarios (semiconductor, PCB, optoelectronics, PV, lithium battery, electroplating); REFERENCE PROJECT = 10 m³/h Shenzhen electronics plant (20% operating-cost cut); RELATED = RO / UF / UV / consumables / lab equipment (CN's "预处理过滤设备" has no EN page — omitted).
- edi-ultrapure-water-system: same skeleton; benefits (no chemical regeneration, 15–18.2 MΩ·cm, 90–95% recovery, unattended, imported GE/EVOQUA/Ionpure stacks); KCEDI-1…100; five application rows; reference = Tianma Microelectronics Wuhan 50 m³/h (named on CN site, 18.2 MΩ·cm, TOC ≤0.3 μg/L, 93% recovery, 3 stable years); RELATED = lab equipment / deionized / double-stage RO / cartridges / consumables (CN's "EDI模块" no EN page — omitted).
- pharmaceutical-purified-water-system: GEO first paragraph kept as a single 138-word citable passage (validate_geo REWRITE_PAGES requires 134–167) with the layered subheads below; CORE BENEFITS module added (GMP compliance, hygienic design, pasteurization, online monitoring, turn-key) + TOC entry; MODEL RANGE = KC-PW-0.5…20; APPLICATIONS = five CN scenarios; RELATED = UPW / EDI / RO / disinfection. CN page has no project case, so no REFERENCE PROJECT module was added.
- deionized-water-system: layered GEO; benefits (1–10 MΩ·cm, 30–50% below EDI, full range, uniform-grade resins, auto/manual regeneration); KCDI-1…100; five application rows; reference = 20 m³/h Dongguan electroplating plant (resin unchanged 3 years); RELATED maps CN's 树脂+滤芯 to the consumables pages. Hero photo still pending from owner.
- laboratory-ultrapure-water-equipment: layered GEO (dual-grade, GB/T 33087-2016 + ASTM D1193 Type I); benefits from CN (18.2 MΩ·cm Type I, dual output, 7-inch screen, <50 dB, cartridge alerts); MODEL RANGE module added (KCLAB-10…100, L/h units) + TOC entry; applications = five CN scenarios; reference = 100 L/h Shenzhen R&D centre central supply (client anonymized per CN-site practice despite the CN title naming it — flagged to owner); RELATED = EDI / deionized / drinking water / double-stage RO / cartridges / consumables.
- All five: breadcrumb intact, hero parameter strips already match CN specs (verified), dateModified 2026-08-19. validate_geo exit 0 (pharma citable passage 138 words), geo_audit clean, site_audit clean; browser DOM checks green on all five.
- Owner follow-up (same day): FAQ answers on pharmaceutical-purified-water-system.html and deionized-water-system.html were aligned with the CN official-site FAQ wording (pasteurization weekly/biweekly at 80–85 °C ≥30 min; IQ/OQ/PQ over 3 weeks then annual revalidation; 8–24 h regeneration cycles; HCl 31% / NaOH 32%; resin life 3–5 years) in both the visible module and the JSON-LD. Editorial leftovers that referenced "the source page" / "the official Chinese page" (2 FAQ answers + a technical-data cell + the process-flow note on the pharma page) were removed; "source page" phrasing is now zero across the site.
- Owner rule (same day): REFERENCE PROJECT and trust statements must not disclose real client names. The EDI page's Wuhan case was anonymized ("a display-panel manufacturer in Wuhan") and the GEO trust bullet now reads "multiple leading electronics manufacturing sites" instead of naming Huawei/ZTE/Tianma. Site-wide scan confirms no other client-name disclosures in page copy (the homepage client logo wall is an approved display).
- Owner follow-up (same day): swept the remaining "The official page specifies/states…" editorial-leftover phrasing (a variant missed by the earlier "source page" scan) — 6 spots on edi-ultrapure-water-system.html (module-life FAQ, feed-water FAQ, hero and documentation intro sentences; visible + JSON-LD) and 1 detail-card sentence on pharmaceutical-purified-water-system.html, all rewritten as direct statements aligned with the CN-site values. "official page" now zero site-wide.

## Five new colour process-flow diagrams installed — 2026-08-19 (pass 14)

Owner supplied Gemini-generated SVG diagrams for the round-1 pages; I optimized them and installed them, replacing the generated P&ID line diagrams.

- Occlusion fixes (owner requirement: no text covered by lines or blocks):
  - industrial-ultrapure (1600×950, 14 nodes): concentrate-return dashed line rerouted along the bottom (y=770) instead of crossing four bottom-row descriptions; node-name font 15→16px.
  - edi-ultrapure: concentrate-discharge line re-anchored from the EDI card's right edge (right-then-down in the inter-card gap) instead of running through the EDI node's own two-line description.
  - pharmaceutical: fully-recirculating loop rerouted via the right margin and bottom (y=620) back into the tank's left edge, clearing the POU/tank descriptions; pasteurization branch recoloured from red to the spec orange #E67E22; node names 15→16px.
  - deionized: regeneration branch now exits the mixed-bed card's left edge and descends in the inter-card gap — removes the dangling arrow that previously pointed into empty space and crossed the node description.
  - laboratory: dispensing-point card shifted 60px left; the Type I / Type II dual-outlet labels moved beside the arrow tips so they no longer sit on the card.
- Installed as `app/images/products/{industrial-ultrapure,edi-ultrapure,pharmaceutical,deionized,laboratory-ultrapure}-process-flow.svg` in the standard pid-figure panel with the PROCESS TRAIN captions retained; alt texts 82–97 chars.
- Verification: all five re-rendered and inspected in-browser after each fix; page-level DOM checks green (image visible, old P&ID gone, caption kept); validate_geo exit 0; site_audit clean. Originals of the Gemini outputs kept at `Desktop/gemini-svg-originals/`; pre-install page backup at `_site_backups/backup_before_flow_svg_install_2026-08-19/`.

## Round 2 — disinfection & consumables family aligned with CN official site — 2026-08-19 (pass 15)

Eight pages aligned to their konchewater.com CN pages (backup: `_site_backups/backup_before_round2_uv_2026-08-19/`). Note: the owner's medium-pressure link pointed at toc-uv.html; the CN site's own medium-pressure page (/products/medium-pressure-uv.html) was used instead.

- toc-uv-degradation-system: layered GEO; CORE BENEFITS added (dual wavelength, ≤0.5 μg/L from ≤50 μg/L feed, imported ≥9000 h lamps, compact retrofit body, 30-year experience); MODEL RANGE added (KC-TOC-05…20 with lamp power and TOC-removal columns); applications replaced with five CN scenarios; RELATED = UPW / EDI / low-pressure UV / lab equipment.
- medium-pressure-uv-system: layered GEO (2–10 kW single lamps, 1–500 m³/h, ≥40 mJ/cm² dose, cooling); benefits added; model range replaced (KC-MUV-2K/4K/10K/20K); applications replaced; RELATED = low-pressure / TOC / ozone / UPW / RO.
- low-pressure-uv-sterilizer: layered GEO (3–5 s physical kill, 0.5–300 m³/h); benefits added; model range replaced (KC-UV-0.5/2…100+); applications replaced; REFERENCE PROJECT added (anonymized Dongguan food plant, in-line UV after RO, stable counts passing audits, low hundreds RMB annual upkeep); RELATED = UPW / RO / UF / consumables / drinking water.
- ozone-disinfection-system (citable 155-word first paragraph): layered GEO; benefits added (6 rows incl. four-duties-in-one); model range replaced (KC-OZ-5G…200G with ozone output and applicable flow); applications replaced (6 rows); RELATED = low-pressure / TOC / medium-pressure / UPW / reclaimed water.
- uv-water-sterilizer (category, citable first paragraph): GEO rewritten — four-series family overview (254/185+254/200–400 nm + ozone, 30–1200 W, 5–500 g/h, 0.5–500 m³/h, 9000–12000 h) with the 3-question layered subheads and series exploration links.
- spare-parts (category, citable first paragraph): GEO rewritten — five-series genuine consumables programme (cartridges/media/resins/EDI/RO), brand compatibility, Shenzhen same-day dispatch, stock/service promises.
- general-water-treatment-consumables: layered GEO; PRODUCT CATEGORIES replaced with the CN nine-row specification table; REPLACEMENT CYCLES replaced (7 rows with influencing factors); applications replaced (6 rows); SUPPLY & SERVICE replaced with the CN six selling points; RELATED = 5 cards.
- uv-disinfection-consumables: layered GEO; PRODUCT CATEGORIES replaced (lamps/ballasts/sleeves/sensors with powers and lifetimes); APPLICATIONS added; REPLACEMENT CYCLES replaced; SUPPLY & SERVICE replaced; a template "Since 1997…" why-konche paragraph that duplicated the general-consumables page was differentiated (UV-specific); RELATED = 6 cards.
- Validation: validate_geo exit 0 after the duplicate-boundary fix; geo_audit 29 pages no flags; site_audit clean; browser DOM checks green on all eight pages. These four UV/ozone pages still carry the generated P&ID diagrams pending the owner's redrawn SVGs (node lists provided).

## AI working-principle diagrams on four UV/ozone pages — 2026-08-20 (pass 16)

Closes the pass-15 pending item: the owner-reviewed working-principle diagrams for the four disinfection pages are finalized and installed.

- Diagrams: ozone / TOC UV (185+254 nm) / medium-pressure UV / low-pressure UV working-principle diagrams (1400×720, light flat style, orange focus node, English labels). Fourteen text-vs-line/block collisions in the delivered drafts were corrected; verified with a headless-Chrome bbox geometry audit (zero collisions) plus rendered screenshots.
- Installation: each page's flow section (COMPLETE PROCESS FLOW / PROCESS FLOW / PROCESS POSITION) now carries the working-principle figure, replacing the compact generated P&ID and its PROCESS TRAIN caption. The redundant WORKING PRINCIPLE sections (heading + 01–05 step cards) and their ON THIS PAGE entries were removed per owner decision.
- Markup follows the pid-figure / pid-scroll / pid-caption convention with role="img" and aria-label; a `.pid-svg.wp-svg` CSS rule keeps diagram typography Arial (layout was measured with it) and raises the mobile scroll floor to 760px.
- Content notes: the MP diagram carries ≥99.99% inactivation consistent with the pass-15 copy; the ozone off-gas label reads "residual O₃ → O₂" (the 20–30 min half-life applies in water and stays in the footer note).
- Validation: in-page DOM geometry audit clean on all four pages; no dead TOC anchors; visual check via rendered section screenshots.
- Sources kept outside the package: fixed SVGs at `outputs/gemini-svg-fixed/`; page backups at `_site_backups/backup_before_wp_diagrams_2026-08-19/`, `_site_backups/backup_before_remove_wp_2026-08-20/`, `_site_backups/backup_before_swap_flow_figure_2026-08-20/`; scripts `_embed_wp_diagrams.mjs`, `_remove_wp_sections.mjs`, `_swap_flow_figure.mjs`.

## Third-party GEO evaluation fixes + dual-layer answer pilot — 2026-08-20 (pass 17)

Independent third-party-rubric evaluation found the package failing its own gates; all fixed in this pass. Backup: `_site_backups/backup_before_geo_fix_2026-08-20/` (8 pages + industrial.css); edit script `../_geo_fix_2026-08-20.mjs` (asserted one-match replacements).

- validate_geo boundary false-positive fixed: the boundary regex could skip past a scope sub-heading and capture the unrelated WHY KONCHE card paragraph on layered pages; it is now anchored (`[^<]*` heading text, `<p>` immediately after). A new answer-lead uniqueness invariant covers layered pages the anchored boundary check cannot reach.
- Citable-word invariant upgraded: REWRITE_PAGES now pass if either the first passage or the combined pre-sub-heading paragraphs sit in the 120–185-word window, supporting both the single-passage and layered (bold "In short" lead + definition-first passage) architectures. general-consumables and uv-disinfection-consumables added to REWRITE_PAGES.
- WHY KONCHE card-01 differentiation: toc-uv, medium-pressure UV, low-pressure UV, ozone and general consumables no longer repeat the verbatim "Since 1997, Konche has accumulated…" template (each now states its page-documented experience scope, following the uv-disinfection-consumables precedent). The remaining 11 pages keep the CN-aligned template pending an owner decision.
- Dual-layer answer pilot (human-scannable + citable): spare-parts' 131-word wall paragraph split into a bold "In short" lead plus a 140-word definition-first citable passage (geo_audit 33→51); general-consumables' second paragraph expanded to a definition-first passage from its own category/replacement tables (33→44); uv-disinfection-consumables opening converted to a definition pattern ("is a genuine-parts supply…", 30→45). All values page-documented; no new claims.
- Homepage client logos: all seven now carry natural width/height (CLS) and descriptive alt text ("BYD — KONCHE water treatment equipment client logo" etc.); site_audit errors and warnings cleared.
- Freshness: dateModified and sitemap lastmod set to 2026-08-20 for the eight touched pages (also covers the pass-16 diagram pages that were still marked 08-19); homepage time element synced.
- Validation: validate_geo exit 0 (28 pages, 28 URLs); geo_audit flags only the pre-existing privacy.html weak passage; site_audit no errors or warnings; browser DOM checks on spare-parts (1440px and 390px: lead/passage/sub-heads visible, no horizontal overflow) and homepage logo accessibility names verified on a local server.

## Dual-layer answer pilot — 2026-08-20 (pass 18)

Owner-approved direction: the first screen is a scan surface ("ad slot, not a manual"), dense citable prose moves to the documentation layer. Pilot on spare-parts; backup `_site_backups/backup_before_dual_layer_pilot_2026-08-20/`; script `../_dual_layer_pilot_2026-08-20.mjs`.

- spare-parts QUICK TECHNICAL ANSWER now shows: bold brand lead (30 words, "KONCHE spare parts: …") + four key-figure chips (5 series / ≥99% rejection / 1–100 μm / 3–10 yr) replacing the in-block passage. Chip styles in industrial.css (`.geo-chips`/`.geo-chip`: silver hairline card, status-green value per the pass-9 value-colour decision, 4-across desktop, 2×2 below 720px).
- The 140-word definition-first passage moved verbatim to the documentation intro ("Keep systems performing." section) with `class="citable-passage"` — a stable marker that decouples the citability invariant from block location.
- validate_geo: `citable-passage` marker governs the 120–185-word window, cross-page uniqueness and definition-pattern checks; the h2-block window check is skipped on such pages. spare-parts passes with the passage in its new location; geo_audit best-passage unchanged (51/100) — moving the passage down cost zero citability.
- site_audit new first-screen prose budget: hero + geo-answer `<p>` prose ≤ 100 words (nav/footer/alt/dl/table/ul excluded). ERROR on converted pages, WARNING elsewhere until rollout. Current sitewide numbers now visible in the audit output (111–314 words on the 26 unconverted answer pages; industry-solutions 314, double-stage-ro 248, ro-category 237 are the densest).
- Browser QA 1440px/390px: four chips render and wrap 2×2 on mobile, lead and relocated passage visible, no horizontal overflow.

Rollout decision pending owner review of the pilot: convert the remaining 26 answer pages to the same pattern (lead + chips + citable-passage in documentation layer), which clears all first-screen-prose warnings.

## Consumables gallery: new photos + stretch fix — 2026-08-20 (pass 19)

Owner supplied `Desktop/images/水处理耗材` (general + UV subfolders). Hash/pixel comparison against installed assets (ffmpeg 32×32 grayscale perceptual diff) showed UV灯1/UV灯2/镇流器1/镇流器2 are the uncompressed sources of the already-installed uv-lamp-range-light/dark, uv-ballast-range and uv-electronic-ballasts WebPs (diff 0.6–7.8) — not re-added. Genuinely new: EDI图片1.jpg and UV紫外线耗材1.jpg.

- New assets (ffmpeg libwebp q82, max 1600px): `app/images/consumables/edi-modules-row.webp` (203KB, 1436×1600, EDI module stacks lined up in the workshop — added as the 10th figure on general-water-treatment-consumables) and `uv-germicidal-ballast-range.webp` (231KB, 1600×1600, germicidal lamps + UBP-series ballast range card — added as the 6th figure on uv-disinfection-consumables). Alt texts 55–61 chars, width/height, lazy loading.
- Gallery stretch fixed (root cause): `.ind-gallery img` previously had no CSS height, so each img's HTML height attribute acted as a presentational hint — width was constrained to 100% but height stayed at the attribute value (filter-cartridges.jpg with height="2000" rendered 383×2000: the "stretched long" complaint). The rule is now `width:100%; height:auto; aspect-ratio:6/7; object-fit:contain; background:#fff` — a deterministic portrait frame matching the dominant 0.87 product-shot ratio: no stretch, no crop (contain), no pre-load collapse and no CLS; figcaptions pin to the figure bottom via margin-top:auto. Note `height:auto` is required — without it the attribute hint overrides the aspect-ratio box.
- Verified at 1440px: all gallery boxes uniform 383×446 (6/7) pre-load and after lazy-load on both pages; gradual scrolling fires the lazy loader normally; 390px: single column, 333×389 boxes, zero horizontal overflow. validate_geo exit 0, site_audit no errors, geo_audit unchanged.
- Backup: `_site_backups/backup_before_gallery_fix_2026-08-20/` (2 pages + industrial.css).

### Owner follow-up (same day)

- Removed the "Range of low-pressure germicidal UV lamps on dark background" figure from uv-disinfection-consumables per owner decision (gallery now 5 figures); asset `uv-lamp-range-dark.webp` deleted (regenerable from `Desktop/images/水处理耗材/紫外线耗材/UV灯2.jpg`).
- Fixed a pre-existing mid-width page overflow exposed during verification: `.detail-intro-row` used `grid-template-columns: 42% 58%` with a 48px gap — percentage tracks plus the gap exceeded the container by 48px, becoming page-level horizontal overflow roughly between 720 and 870px viewports (hidden at 1440 by container margins; gone at ≤720 via the single-column layout). Tracks changed to `minmax(0, 42fr) minmax(0, 58fr)`, which resolves the gap inside the container. Swept 760/840/900/1000/1180/1440 on both consumables pages: zero overflow; validators green.

## Consumables naming unification + hero photos — 2026-08-20 (pass 20)

Owner request: the page said "Spare Parts" while the navigation said consumables; unify, and add photos to the consumables product-page heroes. Unified on **"Water Treatment Consumables"** (the header-nav label on all 29 pages, and the family name matching both child pages); the `spare-parts.html` URL and the two-term `<title>` deliberately stay for search coverage. Backup `_site_backups/backup_before_rename_hero_2026-08-20/` (11 pages); script `../_rename_hero_2026-08-20.mjs`.

- Renamed to "Water Treatment Consumables": spare-parts H1, its visible breadcrumb segment, the engineering nameplate model line (MODEL KONCHE SP · WATER TREATMENT CONSUMABLES PROGRAM), the footer dropdown label on the 9 pages that still said "Spare Parts", the homepage Products-grid card, JSON-LD CollectionPage/ItemList names and the BreadcrumbList parent segment on all three consumables pages, and the llms.txt entry title. Page body copy intentionally keeps "consumables and spare parts" phrasing (both search intents covered in content).
- Hero photos (both pages previously showed the legacy CSS orbit graphic): spare-parts now carries the workshop EDI-row photo (`edi-modules-row.webp`, cover frame) and general consumables carries the filter-cartridges studio shot (`filter-cartridges.jpg`, contain frame per the pass-5 white-background convention), promoted from its gallery — the gallery figure was removed to avoid same-page duplication (now a clean 3×3 of nine figures). Both hero images eager + fetchpriority, width/height and descriptive alt per audit rules.
- Freshness: dateModified and sitemap lastmod set to 2026-08-20 on all 13 touched pages.
- Validation: validate_geo exit 0; site_audit no errors; geo_audit unchanged (spare-parts 51, general 44). Browser QA at 1440 and 390: H1/breadcrumb/nameplate/footer/nav labels consistent sitewide, both hero photos load, no horizontal overflow; homepage card label verified.

## Dual-layer rollout to all product pages — 2026-08-20 (pass 21)

Owner approval of the spare-parts pilot extended to every remaining product/solution page (23 pages; script `../_dual_layer_rollout_2026-08-20.mjs` with per-page asserted data; backup `_site_backups/backup_before_dual_layer_rollout_2026-08-20/`).

- Every converted page now shows in QUICK TECHNICAL ANSWER: a bold brand-anchored lead of 23–32 words ("KONCHE Double Stage RO: two-pass desalination to ≤2 μS/cm …") followed by the existing scannable sub-heads, lists and tables. The definition-first citable passage (120–185 words, page-documented values only) moved to the documentation-layer intro, marked `class="citable-passage"` on 18 pages (REWRITE members plus the wall pages); layered non-REWRITE pages moved their support+typical paragraphs down unmarked.
- Four short passages were extended into the 120–185 window from their own documented tables before moving (single-stage RO 81→130, reclaimed 58→121, hollow-fiber UF 54→125, UV consumables 99→122 words). Five passages needed a one-phrase definition-pattern fix after the move (general consumables, ozone, pharmaceutical, reclaimed, UV category) — all now open with "X is a …".
- Wall-page scope boundary paragraphs became bullet lists under their sub-heading (sentence-preserving split). The solutions hero paragraph was trimmed from 113 to 49 words (all facts kept). No chips were added on these pages: each already carries the numeric hero parameter strip, which serves the key-figure-card role (the pilot page needed chips only because its strip is qualitative).
- site_audit CONVERTED_PAGES now enforces the first-screen prose cap (≤100 words of hero + answer paragraphs) as an ERROR on all 24 product/solution pages. Measured result: first-screen prose fell from 111–314 words to 29–48 words per page; the only remaining warnings are the non-product index/about/contact pages, deliberately out of scope.
- llms-full.txt's answer-architecture description updated to the two-layer model. dateModified and sitemap lastmod set to 2026-08-20 on all touched pages.
- Validation: validate_geo exit 0 (28 pages, 28 URLs, all citable passages in window, unique, definition-first); site_audit no errors; geo_audit unchanged except the pre-existing privacy.html flag. Browser QA at 1440/390 on double-stage RO, EDI, RO category, industry-solutions and EDI mobile: leads render bold, passages sit in the documentation layer, no horizontal overflow.

### Owner follow-up (same day): homepage converted

- The homepage "What water treatment systems does KONCHE provide?" block now carries a bold brand lead (~30 words: "KONCHE: industrial and commercial water treatment equipment since 1997 — …") plus the quote-scope sub-heading with its boundary text as two bullets. The 129-word definition passage moved verbatim to the OUR SERVICE section intro, marked citable-passage.
- index.html added to the site_audit CONVERTED_PAGES enforcement set; first-screen prose dropped from 219 to ~72 words (hero 30 + lead 30 + links). geo_audit best passage unchanged (58/100). Validators green; browser-verified at 1440. About and contact keep their single-passage blocks (company pages, owner may request later).

## Premium industrial + cinema-hero design layer — 2026-08-20 (pass 22)

Owner direction: clean and rational, direct value statements, atmospheric cinematic heroes on the homepage and solutions page, weaker catalog feel on product pages. Implemented as two CSS layers in industrial.css (§20 premium base, §21 cinema), applied through body classes — fully reversible by removing the classes.

- §20 premium base (body class `premium-demo` on all 29 pages): section rhythm 116→96–150px clamp; display headings up to 52–56px with tighter tracking; the answer lead becomes a display element (17–21px, weight 560, navy); hairline table discipline (light header + 2px navy top rule, zebra retired, hairline row rules); cards and FAQ items on white with #E2E8EA hairlines and a quiet hover lift; uppercase tracked primary buttons; engineering-plate serial line retired (de-shelf).
- §21 cinema heroes (body class `cinema-hero` on index and industry-solutions only): homepage banner re-graded from the light scrim to a deep-navy cinematic gradient (white display H1, orange #F5A942 accent words, white secondary text); solutions hero gains the containerized-installation scene photograph under a navy scrim with white type (breadcrumb, subtitle, parameter values). Product heroes deliberately stay light — only the two brand pages go cinematic.
- Solution modules: clean-rational white cards (was tinted #f8faf9), 38px padding, wider micro-tracked module headings, hairline grids; solutions intro display heading to 56px.
- Validation: CSS brace-balanced; validate_geo 28/28, site_audit no errors, geo_audit unchanged. Browser-verified computed styles and contrast on solutions, homepage and double-stage RO at 1440; zero horizontal overflow on all three plus 390px mobile sweeps. Pre-change backups: `_site_backups/backup_before_dual_layer_rollout_2026-08-20/industry-solutions.pre-premium.html`, `industrial.pre-premium.css`.

## Five hero photographs installed — 2026-08-20 (pass 23)

Owner supplied six photos with assignments (3× UF renders, medium-pressure UV, deionized water, industrial ultrapure render). Five installed; the sixth (UF render `exec-40e35203`) deliberately held back because the rendered membrane modules carry a visible third-party-style brand label ("GEROTEC") — flagged to the owner for re-issue or approval to crop.

- New WebP assets in `app/images/products/` (libwebp q82, no upscaling, all under 150KB): `uf-treatment-skid.webp` 1448×1086, `uf-system-modules.webp` 1086×1448, `uv-mp-sterilizer-units.webp` 1440×1080, `deionized-water-system-skid.webp` 667×500, `industrial-ultrapure-system.webp` 1448×1086.
- deionized-water-system, uf-ultrafiltration-system (category) and uf-water-treatment-system: legacy CSS orbit hero graphics replaced with photo frames (ind-cover 420px for the two landscape shots; ind-portrait 500px pattern from the RO category page for the hollow-fiber portrait). These were the last orbit-graphic product heroes in the catalog.
- uv-water-sterilizer: hero swapped from the low-pressure flow-through studio shot (duplicated from low-pressure-uv-sterilizer) to the real medium-pressure unit photo; frame moved contain→cover since it is a scene shot. The LP photo remains in use on its own page.
- industrial-ultrapure-water-system: hero swapped from the PCB installation photo (still used by index and electronic-grade pages) to the dedicated ultrapure train render.
- All five imgs eager + fetchpriority=high, intrinsic width/height, descriptive alt 78–102 chars. dateModified normalized to 2026-08-20. Backup: `_site_backups/backup_before_5hero_images_2026-08-20/` (5 pages).
- Known limits flagged to owner: the deionized photo is 667×500 (acceptable at DPR1, soft on retina — a larger original would be welcome); UF and ultrapure heroes are AI-generated renders, consistent with the owner's supply choice.
- Validation: validate_geo exit 0; site_audit no errors (only the standing about/contact advisory); geo_audit unchanged. Browser QA at 1440 and 390: all five heroes load (naturalWidth>0), correct frame classes/sizes (529×420, 529×500 portrait; 275×230, 275×279 mobile), no orbit graphics remain, zero horizontal overflow.

## Flow diagrams completed for the last two pages — 2026-08-20 (pass 24)

Owner generated the two remaining process diagrams with the prepared prompts and supplied the SVGs. This pass adjusted, installed and verified them — every page with a process/principle diagram now uses the same 1600×720 visual language.

- Adjusted owner-supplied SVGs to series conventions: node labels 14→16-17px, equipment tags 12→14px, titles 36→40px, sub-captions 18→20px, bottom spec line 14→16px, small annotations (185/254 nm, MB-01, Concentrate) raised to 12-14px — matching the 16-17px label / 40px title baseline of the nine existing diagrams.
- De-obscuring fixes: UF "POINT OF USE / To RO feed / Reuse" moved below the flow band and right-anchored to stay inside the 1600 canvas (previously ran off the right edge); UF optional-stage box widened 130→170px so 16px two-line labels fit; backwash label lifted clear of the dashed return line.
- Installed as `app/images/products/uf-water-process-flow.svg` and `electronic-grade-process-flow.svg`; the legacy inline `pid-svg` strips on uf-water-treatment-system (COMPLETE PROCESS FLOW) and electronic-grade-ultrapure (TYPICAL PROCESS ARCHITECTURE) replaced with the standard `<figure class="pid-figure"><img class="flow-diagram-svg" …></figure>` pattern used by the other nine pages (lazy, 1600×720, descriptive alt). Adjusted copies also written back to the owner's Desktop files (originals kept as *.orig.svg).
- Site-wide diagram inventory after this pass: 11 pages on 1600×720 external diagrams, 4 UV pages on 1400×720 inline wp-svg, zero legacy strips remaining.
- Validation: validate_geo 28/28, site_audit no errors, geo_audit unchanged. Browser QA at 1440/390: both figures load after scroll (lazy), no horizontal overflow, old strips gone.
- Session note: a second local http-server (bound to 127.0.0.1:8642, serving the other working copy) was shadowing this directory's preview; both instances were killed and one clean no-cache server restarted on 8642 for this copy.

## GPT-session delta merged — 2026-08-20 (pass 25)

The parallel working copy (`../UltraPure_v2.0/`, no `_GLM53` suffix) had received an afternoon session of GPT edits on top of this directory's pass-23 state (synced in with mtimes preserved). Per owner approval the full delta was merged back here; the other copy is now an archive and should not be edited further.

- Merged 18 files verbatim (my copies unchanged since the sync): homepage carousel + client-logo re-layout + 2-column citable restyle, industrial.css §service-carousel/buyer-card/product-documentation (+222 lines), site-enhancements.js initServiceCarousel (+70 lines), and parameter unification across 13 product pages (RO 97–99%, brackish RO 0.8–1.5 kWh/m³, UF 0.3–0.7 kWh/m³ and 2–5 yr membrane life, UV "up to 99.99%" and 8,000–10,000 h lamps, containerized 30–40% savings, conditions-disclaimer sentences on 5 pages; zero ≥99%/99.2–99.8% leftovers verified).
- Three-way merged 2 pages (uf-water-treatment-system, electronic-grade-ultrapure-water-system): GPT's copy taken, then the pass-24 1600×720 flow figures re-installed over the legacy strips — buyer/parameter edits and new diagrams coexist.
- Carousel assets: 01/03/04/06 recompressed to WebP q82 (6.7MB → 437KB) with index.html references updated; unreferenced legacy 06 jpg dropped. 02/05 kept as reasonable-size jpg.
- Known open items carried from review: new energy/lifetime/savings figures lack a documented source (GEO-FACT-CHECK.md not updated — owner to confirm or supply CN-site references); citable passages now sit inside collapsed <details> product-documentation blocks (accepted tradeoff, AI crawlers read raw HTML).
- Validation: validate_geo 28/28; site_audit no errors (standing about/contact advisories only); geo_audit unchanged. Browser QA: homepage carousel loads and switches on click (01→02, counter 02/06, active row sync), 7 client logos, six buyer cards with Effectiveness/Energy/Cost heads, both flow figures loading on the three-way pages, no horizontal overflow at 1440 or 390.
- Backup of pre-merge state: `_site_backups/backup_before_gpt_merge_2026-08-20/` (18 files).

## Card alignment fix + seventh solution — 2026-08-20 (pass 26)

Two owner-reported items after the merge:

- **Ultra-Pure Water Systems buyer cards fixed**: on electronic-grade-ultrapure-water-system.html the six buyer cards sat inside the 58% column of the detail-intro-row two-column layout (narrow and left-shifted). The buyer-card-grid block was moved out to sit directly under .container.detail-container, matching the other five category pages. Browser-verified: grid now spans the full 1180px container, 3 columns, flush left edge.
- **Electronics & Precision Manufacturing Ultrapure Water Solution added** to industry-solutions.html as SOLUTION 02 (inserted after the general Industrial Pure Water & Ultrapure Water article; later articles renumbered 03–07). Content follows the existing five-section module template (Overview / Application Scenarios / Main Pain Points / Recommended Equipment / Treatment Outcomes) with values documented on the industrial-ultrapure, EDI and TOC-UV pages (18.2 MΩ·cm, TOC 0.5 μg/L, RO + EDI + 185/254 nm UV polishing, circulating loop) and equipment links to those three systems. llms.txt updated from "six" to "seven" solution directions.
- Validation: validate_geo 28/28, site_audit no errors. Browser QA: seven solution articles render with continuous numbering, new equipment links resolve, no horizontal overflow.

### Owner follow-up (same day): homepage tab for the new solution

- Homepage INDUSTRY SOLUTIONS tab list extended 4 → 5: "Electronics & Precision Ultrapure Water" tab added (data-index 4) with its industrySolutions entry (label ELECTRONIC-GRADE ULTRAPURE WATER, 18.2 MΩ·cm / TOC-control description) and indPhotoMap entry using `industrial-ultrapure-system.webp` (not previously used on the homepage; the PCB photo remains on the General Industrial Pure Water tab).
- validate_geo 28/28, site_audit no errors. Browser QA: fifth tab click switches active state, aria-label, image label, description and photo (loads correctly), no overflow at 1440.

### Owner follow-up (same day): typical-value provenance recorded

- The pass-25 open item (new energy/lifetime/savings figures without documented source) is closed for now: the owner approved recording them as owner-confirmed typical reference values. GEO-FACT-CHECK.md now carries a dated table of all eight values (RO 97–99%, 0.8–1.5 / 1–2 / 0.3–0.7 kWh/m³, UF membrane 2–5 yr, UV up to 99.99% and 8,000–10,000 h lamps, containerized 30–40% with "potential" qualifier retained). validate_geo still green.

### Owner follow-up (same day): containerized savings claim removed

- The "30–40% potential investment savings" figure was removed sitewide at owner request (single occurrence: the containerized page's Cost buyer card). Replaced with a qualitative statement (factory assembly and containerized delivery reduce on-site construction scope and shorten installation). GEO-FACT-CHECK.md row struck through with a do-not-reintroduce note. validate_geo and site_audit green.

## Navigation regroup + floating product menu + insights removed — 2026-08-21 (pass 27)

Three owner-directed structural changes:

- **Products mega-menu**: the seven top-level category dropdowns were collapsed into a single "Products" item (right after Home) on all 28 nav-bearing pages, using the legacy dropdown-group/dropdown-submenu pattern whose CSS was already present. Order now: Home / Products / Solutions / About Us / Contact Us. The active category is marked inside the menu on its pages. The "Resource" nav item was removed together with its target.
- **Industry Insights module deleted** from the homepage (the 4-card section linking into product docs); zero residual #insights references remain site-wide.
- **Floating product quicknav** on every page (28 pages): a navy vertical rail on the right edge (below the homepage social dock position, z-index above it) that expands into a panel with all 7 categories and 17 sub-products, current page highlighted (category and sub level); on mobile it becomes a circular button at the bottom-right opening a bottom sheet. Self-contained: the markup carries an inline idempotent init script — no dependency on the per-page JS files (an external-file approach failed on the homepage due to a script-loading anomaly; inline proved robust). Verified on desktop and 390px: open/close, outside-click and Esc close, no horizontal overflow; mega-menu hover shows all 7 groups.
- Validation: validate_geo 28/28, site_audit no errors. Typography audit data collected for the pending unified type-scale proposal (not yet applied).

### Owner follow-up (same day): single-row header on large screens

- §24 override in industrial.css: above 1180px the nav-v2 header drops its legacy two-row grid (64+68px, designed for the old nine top-level items) for one 80px row — brand | centered nav | Request a Quote — matching the already-single-row layout used at and below 1180px. Nav item font 15→13.5px with nowrap; dropdown panels re-anchored to the 80px header bottom. Verified at 1440/1250 (one line, mega-menu opens correctly) and 390 (mobile drawer unchanged).

### Owner follow-up (same day): quicknav moved to the left edge

- Floating product rail moved from right to left edge (§23 adjustments: side, rounded corners, shadow direction; panel now expands rightward). Mobile FAB now bottom-left, clear of the social dock (bottom-right). Verified 1440 (rail at x=0, panel opens rightward, dock unaffected) and 390 (FAB bottom-left, no overflow).

## Information-architecture expansion — 2026-08-21 (pass 28)

Owner-specified IA rollout: 8-item top nav, two new pages, About restructure, homepage partner module.

- **Spare Parts & Sourcing Service page** (products/spare-parts-sourcing-service.html): dual-layer GEO pattern (bold lead + components/boundary sub-blocks; 148-word citable passage in a documentation details block), six service-highlight cards, boundary statement per owner copy, and a five-field mailto inquiry form (component/quantity/requirement/project/contact). Own active nav state; quicknav included.
- **Resources page** (resources.html): Technical Articles (six cards linking to real selection/comparison/process sections on product pages), General FAQ (five new general questions with visible parity + FAQPage schema), Download Center (four document cards with mailto request flow — real PDFs pending owner supply).
- **About Us restructured** into six owner-specified blocks: Company Introduction, Factory & Production Capacity, Quality Control Process (4 steps), Certifications (ISO 9001/14001/45001, mirroring homepage wording — certificate copies still pending), Our Team (role-based, no invented names), Our Service Promise (5 lifecycle commitments incl. spare-parts sourcing).
- **Top nav expanded to 8 items** site-wide (industry-solutions.html keeps its minimal landing-page nav by design): Home | Products | Spare Parts & Sourcing Service | Solutions | Projects | About Us | Resources | Contact Us. Projects points to the homepage track-record section (#about) until dedicated case material is supplied.
- **Homepage Why Partner With Us module** added after OUR SERVICE: six cards (factory-direct supply, custom engineering, project delivery, technical support, spare-parts supply chain, single accountability), site-documented facts only.
- Plumbing: sitemap.xml and llms.txt extended (+2 URLs, now 30 pages); sourcing/Resources meta descriptions tuned to length; duplicate-id fix on About; inherited active-mark fix on the sourcing page.
- Validation: validate_geo 30/30, site_audit no errors (standing about/contact first-screen advisories remain). Browser QA: 8 nav items confirmed on one row at 1440/1250/1181; sourcing page single active state; six partner cards, six highlight cards, five form fields, six article cards, five FAQ items, four download cards all render; no horizontal overflow at 1440 or 390 on the new/restructured pages.

## Brand-green accent unification — 2026-08-21 (pass 29)

Owner correction: the brand color is deep green #015368, not the orange accent family. Sitewide UI accent swapped (backup: `_site_backups/backup_before_brand_green_2026-08-21/`):

- industrial.css: 78× #F47A22 → #015368 (eyebrow dashes, hovers, rules, rails, toggles); 14× #D96612 → #013A4D (hover-dark); 2× #D85F12 → #015368 (buyer-card strong); 1× #F5A942 → #4ECFB4 (cinema-hero accent word — light-green variant kept for contrast on navy, ≈8.9:1); 8× #3F9142 → #015368 (hero parameter values).
- globals.css: --aqua and 19 literals #479544 → #015368 (original site green aligned to brand green).
- Sourcing-page inline form styles updated. Process-diagram SVG equipment highlights intentionally keep their technical palette (content, not UI chrome).
- Verified: hero accent rgb(78,207,180), eyebrow dash / param values / buyer strong all rgb(1,83,104); #015368 on white ≈8.5:1. validate_geo 30/30 still green. Typography proposal accent token updated to #015368 accordingly.

### Owner follow-up (same day): accent aligned to the actual logo green

- Pixel-sampled `app/konche-logo.png`: the real logo is two-tone — blue #0c599a + green #009944. The previously supplied #015368 did not match the logo, so all accents were re-aligned to the sampled brand green: #015368→#009944 (107 occurrences across industrial/globals CSS + sourcing inline), hover-dark #013A4D→#00763A (≈5.7:1 on white), cinema-hero light accent #4ECFB4→#57D98F (≈9.6:1 on navy). The top-left CSS brand-mark bars now render exactly rgb(0,153,68). Logo blue #0c599a recorded as available brand color (not currently used in the UI; navy #071C2C plays the dark role). validate_geo 30/30 green.

## Typography system v1 applied to the homepage — 2026-08-21 (pass 30)

- Owner confirmed the type-scale proposal (accent updated to logo green) and asked to apply it to the homepage first. Implemented as §25 scoped to `body.type-v1` (homepage only for now; roll out page-by-page by adding the class).
- Verified computed styles at 1440: hero H1 64px/w600, all section and geo H2 unified to 46px/w600/#071C2C (previously 46/48/51.8/54 mixed), dark-section H2 stays white, eyebrows 11px caps, answer lead 21px/w800, section body #4A575E, buyer-card H3 16px, about-values H3 21px/w700, service rows 21px/w600 in ink. Mobile: H1 clamps to 44px at 390, no overflow. Logo blue intentionally not introduced (owner decision); navy skeleton + green accent retained.

## Typography v1 rolled out site-wide — 2026-08-21 (pass 31)

- `type-v1` body class extended to all 30 pages. §25.1 adds protective restores so the unified H3 rule does not blow up label-style headings (geo-answer-sub stays 12.5px caps, solution-module labels 15px caps) and normalizes per-context heads: detail-section/process H3 19px/w700, product-section-card H3 21px, insight-card H3 20px, product/landing hero H1 clamp(40–64px)/w600 ink; detail-section-heading H2 forced to the unified 46px scale and hero H1s migrated to ink #071C2C.
- Bug found and fixed while rolling out: the two pass-28 generated pages (resources, spare-parts-sourcing-service) were missing their <body> tag entirely — they had been rendering without premium-demo styles. Proper body tags inserted.
- Spot-verified at 1440 across product / solutions / about / resources / sourcing pages: unified sizes/weights/colors applied, protected labels intact, no overflow; mobile H1 clamps to 40px, no overflow. validate_geo 30/30, site_audit no errors.

## Homepage geo-answer two-block layout + ABOUT type fix — 2026-08-21 (pass 32)

- "What water treatment systems does KONCHE provide?" answer block restructured: the quote-scope bullets replaced by two low-saturation-green blocks (#E9F3EC fill, #D3E7DA hairline, green top rule) — 01 Standard & Custom Equipment carrying the six category names as compact pill links, 02 Spare Parts & Component Sourcing with a one-line description and CTA to the sourcing page. A short scope sub-heading ("Two directions to work with KONCHE") kept before the blocks to satisfy the geo-answer validator contract; the quote-boundary content remains covered by the services-layer citable passage.
- ABOUT KONCHE module normalized: kicker 16px gray → 11px brand-green caps; h2 second line's muddy gray-green (#6C8F78) → brand green #009944, matching the hero accent pattern.
- Verified at 1440/390: two blocks render (6 chips + CTA), mobile stacks to one column, no overflow; validate_geo 30/30 and site_audit green.

## Unified hero parameter slots + buyer-card CTAs — 2026-08-21 (pass 33)

- Owner chose option B (standardized slots) minus the duty slot: every product hero strip now uses the fixed semantic schema **Capacity → Output → Process** with sitewide-identical labels, values taken only from each page's own documentation. Pages without a documented slot value omit it gracefully (UF/ozone/containerized carry two slots; containerized keeps a single Process slot). Consumables pages (spare-parts, general, UV consumables) have no capacity/output/process semantics — their qualitative strips were removed per owner direction.
- All six buyer cards on the six category pages now carry an inquiry CTA (previously only Where-it-fits and Effectiveness did): cards 01/02/05/06 gained "Ask an engineer / Request a quote / Ask for an estimate / Request a quote" CTAs linking to the contact page.
- Verified in browser: RO strip reads Capacity/Output/Process in order; UV and ozone two-three-slot variants render; consumables strips gone; six CTAs present per category page; no overflow. validate_geo 30/30, site_audit green.

### Owner follow-up (same day): homepage FAQ pruned

- "How do I choose between single-stage and double-stage RO?" removed from the homepage FAQ — both the visible details item and its JSON-LD Question entry (the Resources page keeps its own distinct version of this guidance). All remaining JSON-LD blocks re-validated; validate_geo 30/30.

### Owner follow-up (same day): Projects nav item removed

- The "Projects" nav item (a placeholder pointing at the homepage ABOUT KONCHE anchor) was deleted from all 31 page navs at owner direction — the semantic link was wrong. No other content touched; nav is now Home / Products / Spare Parts & Sourcing Service / Solutions / About Us / Resources / Contact Us. validate_geo 30/30 green.

### Owner follow-up (same day): sitewide nav consistency pass

- Full nav audit vs the homepage revealed and fixed: (a) 11 pages still pointing Resources at the retired index#faq anchor → all now link resources.html; (b) about.html missing Resources + About Us, resources.html missing Resources + About Us — inserted with proper active states; (c) industry-solutions.html (the landing-style page) brought up to the full standard nav with Solutions marked active; (d) the homepage itself had lost its Home link at some earlier edit — restored. Final audit: all 31 pages carry the identical seven-item nav (Home / Products / Spare Parts & Sourcing Service / Solutions / About Us / Resources / Contact Us) in the same order. validate_geo 30/30.

### Correction to the nav-consistency note

The previous entry overstated completion; a final attribute-order-agnostic audit found three residual duplicates (an extra About Us in the about and resources main navs, an extra Solutions in industry-solutions) introduced during the fix itself. All removed; the final audit confirms ALL 31 pages carry the identical nav: Products | Home | Spare Parts & Sourcing Service | Solutions | Resources | About Us | Contact Us (Home resolves to #top on index). The homepage never actually lost its Home link — its class-first markup had defeated the first audit regex. validate_geo 30/30, site_audit green.

## Hero parameters rewritten from the official site — 2026-08-21 (pass 35)

- All 19 product hero strips now mirror the official konchewater.com product pages (fetched 2026-08-21), replacing the generic Capacity/Output/Process slots with the official per-product parameter names: e.g. ultrapure = Resistivity ≥18.2 MΩ·cm / TOC ≤0.5 μg/L / Capacity 0.5–100 m³/h; RO = Salt rejection ≥99% / 0.25–500 m³/h / PLC automatic; LP UV = ≥99.99% / 0.5–300 m³/h / 9,000 h lamp / 254 nm. 吨/小时 rendered as m³/h; pharma carries the official four parameters; containerized keeps its single Process slot (no official page).
- Consistency sweep: 24 body occurrences of the old unified "97–99%" RO rejection replaced with the official "≥99%" across 7 files (RO pages, homepage citable passage, consumables references, UF category) — hero and body no longer contradict. Note: official LP lamp life is stated as 9,000 h (point value); body keeps the 8,000–10,000 h range, which contains it — recorded as compatible.
- GEO-FACT-CHECK provenance: hero strips now carry official-site sourcing (konchewater.com product pages, 2026-08-21). validate_geo 30/30; strips verified rendering via served HTML.

### Owner follow-up (same day): RO rejection set to 97–99%

- Owner overrode the official-site ≥99% figure: all RO salt-rejection references (hero strips on the three RO pages + body/citable/table/comparison occurrences across 7 files, 27 in total incl. three pre-existing ones) now read 97–99% consistently; ≥99.99% sterilization values untouched. GEO-FACT-CHECK row annotated with the decision and the official-site divergence. validate_geo 30/30.

## Homepage category directory bar restored + reference-project panel re-skinned — 2026-08-22 (pass 36, css v20260822-6)

Owner reported two issues; both fixed.

- **Homepage top directory restored.** The 2026-08-21 nav/quicknav refactor had collapsed the header's product-category row into a single "Products" dropdown plus the floating quicknav rail; the owner wanted the visible category directory back on the homepage. The homepage header is now two rows again (≥1181px): row 1 keeps the standard seven-item nav (Home / Products / Spare Parts & Sourcing Service / Solutions / Resources / About Us / Contact Us + Request a Quote), row 2 is a new `nav-catbar-row` listing the quicknav's seven level-1 categories (Reverse Osmosis System, Ultra-Pure Water Systems, Laboratory Ultrapure Water System, Containerized Water Treatment System, Ultrafiltration System, UV Water Sterilizer, Water Treatment Consumables) — each with its hover/focus dropdown of the quicknav's sub-items. Label parity with the quicknav asserted programmatically; all 18 links verified on disk. Below 1181px the row is hidden (mobile drawer + quicknav cover it). Laboratory is a direct link (its old sub-item duplicated itself); row-1 Products mega-dropdown re-anchored to drop from the taller header bottom. `site-enhancements.js` now binds `#navCatRow` submenu toggles alongside the main nav's. Other pages keep the single-row header.
- **REFERENCE PROJECT panels readable.** `.detail-case` (the "Representative KONCHE … projects" block on single-double-stage-ro, industrial-ultrapure, laboratory-ultrapure and low-pressure-uv) still carried the legacy globals.css navy card (`background: #081b33`, white text) while §25.1 typography forced dark-ink headings/paragraphs onto it — dark-on-dark, unreadable, and off-theme. §27 in industrial.css re-skins it as a light panel (#E9F3EC fill, #D3E7DA hairline, 3px #009944 top rule, rounded) with ink headings, #4A575E body and re-colored meta-grid — matching the geo-answer block language used on the homepage. Pure CSS; no HTML changes on the four pages.
- Housekeeping: stray `</script>` after the site-enhancements include in index.html removed; asset query bumped to v=20260822-6 across all 32 pages.
- Validation: site_audit 32 pages no errors (standing about/contact first-screen advisories only); validate_geo 31 pages / 25 sitemap URLs green; regression_20260822 49/49 PASS. Headless-Chrome QA at 1440/1250/390: two-row header renders, all seven categories readable in one bar, no overlap or clipping; reference-project panel verified light-green with readable text; mobile header unchanged with the directory hidden.

## Category bar sitewide + orange header CTA + capability CTA consolidated — 2026-08-22 (pass 37, css v20260822-7)

Owner follow-up on pass 36: the category directory is wanted on every page, the header Request-a-Quote button goes back to orange, and the PRODUCT CAPABILITY card links are too many.

- **Category directory bar on all pages.** The pass-36 homepage-only `nav-catbar-row` (§26) now ships on all 26 live pages (5 root + 20 products + index; the 6 redirect-stub pages are untouched). Root pages link `products/…`, product pages link siblings — every link programmatically verified on disk. The §24 single-row override applies as before on other pages; with `nav-catbar` the header becomes 80px + 58px two-row everywhere ≥1181px. The quicknav rail stays as-is.
- **Request a Quote back to orange (§28).** Owner re-request supersedes the pass-29 green CTA: header `.header-cta` is now #F47A22 (white text, white-on-hover as before). Scope is the header button only — body accents (eyebrows, buttons, links) remain logo-green #009944 per pass 29/30. The mid-band 721–1180px previously hid the CTA entirely behind the hamburger layout; it now shows a compact orange CTA beside the hamburger.
- **PRODUCT CAPABILITY CTAs consolidated (§29).** The six per-card `buyer-card-link` CTAs added in pass 33 were removed on the five category pages (ro, containerized, laboratory, uf, uv — 30 links); each module now ends with one green "Request a Quote ↗" primary button under the last card row, linking the contact page. Dead `.buyer-card-link` CSS rules removed.
- Preview note: the local server had been stopped after pass 36 (owner saw a stale/dead link — the on-disk archive copy `../UltraPure_v2.0/` still carries the old orange design and no pass-36/37 fixes; do not serve or edit it). Server restarted on 8642 with caching disabled; hard-refresh recommended.
- Validation: site_audit 32 pages no errors (standing about/contact advisories); validate_geo 31/25 green; regression_20260822 49/49 PASS. Headless QA at 1440 (homepage + RO page: two-row header, orange CTA, 7 categories, single module CTA, cards clean), 900 (brand + orange CTA + hamburger, no overlap), 390 (drawer layout unchanged, no overflow).

## First-level nav clarified: single row, CTA far right — 2026-08-22 (pass 38, css v20260822-8)

Owner clarification: the pass-36/37 second category row was a misread. The wanted header is ONE first-level nav row — Home / Products / Spare Parts & Sourcing Service / Solutions / Resources / About Us / Contact Us left-to-right, orange Request-a-Quote at the far right.

- **Category directory bar removed** from all 26 live pages and its §26 CSS deleted (the JS submenu binding is self-guarding and stays).
- **Nav left-aligned (§24 tweak):** above 1180px the seven items now flow left-to-right right after the brand (was centered); CTA rightmost via the 3-column grid.
- **Single-row nav extended down to 780px (§30):** between 780–1180px the header stays one row — 11.5px nav font, brand subtitle hidden, hamburger suppressed, desktop hover dropdowns restored (the old ≤1180px drawer rules fully overridden). The owner's browser pane is below 1180px, which is why the nav had appeared "missing" in earlier rounds. 780px is the floor: all seven items + CTA fit verified at 800px.
- **≤779px:** drawer returns, compact orange CTA kept visible beside the hamburger (CTA rightmost; hidden only below 380px).
- **PRODUCT CAPABILITY module CTA now orange (§31):** #F47A22 with #D96612 hover on the five category pages, matching the header button.
- Validation: site_audit 32 pages no errors; validate_geo 31/25 green; regression 49/49 PASS. Headless QA at 1440/1000/800 (single row, correct order, orange CTA far right, no wrap/overlap), 500 (brand + hamburger + orange CTA rightmost), and the RO PRODUCT CAPABILITY module (single orange button, clean cards).

### Owner follow-up (same day): nav overlap in the 780–1180 band fixed (css v20260822-9/10)

- Symptom: on real viewports around 820–1050px the "Contact Us" / "About Us" items slid under the orange CTA (owner screenshot report).
- Root cause, measured with a getBoundingClientRect probe on a header-only copy of index.html: globals' `.nav-v2 .main-nav > a {flex: 0 0 auto;}` forbids shrinking, so the long "Spare Parts & Sourcing Service" item kept single-line width and pushed its neighbours into the CTA. Probe confirmed overlaps at vw 828/878/928/978 and clean layout at ≤800 and ≥1078.
- Fix (§30 amendments, v9+v10): nav items in the band may now shrink (`flex: 0 1 auto`, `min-width: 0` — including `.nav-category`), so long labels wrap to two lines inside their own cell; font clamps 10.5–12px with 1.25 line-height; brand subtitle hidden ≤1180px (incl. ≤779 band) to free width; nav gap tightened.
- Verification: geometry probe re-run at 12 widths from 780 to 1440 plus 500/390 — zero overlaps, zero overflows everywhere; visual check at 900px shows "Spare Parts & Sourcing Service" wrapping cleanly, nothing clipped. site_audit/validate_geo green; regression 49/49. Probe harness deleted after use.

### Owner follow-up (same day): evenly distributed nav + gradient separators (css v20260822-11)

- First-level items now spread evenly (space-between) across the space between the brand and the orange CTA in both desktop bands (≥1181px and the 780–1180px band); item order unchanged, gap floors retained so wrapping behaviour from v10 is unaffected.
- Six separators (one between each pair of adjacent items, 7 items → 6 lines): 1px vertical hairline, 20px tall, vertically centered in the gap, light gray #AFBEC5 with alpha fade at both ends (dark middle, transparent tips), scoped to ≥780px so the mobile drawer is untouched.
- Verified: geometry probe at 9 widths 780–1600 — zero overlaps, 6 separators active everywhere in band; visual checks at 1440 and 1000 confirm even spread, gradient tips, no clipping. site_audit / validate_geo green, regression 49/49.

### Owner follow-up (same day): minimal B2B industrial header restyle (css v20260822-12)

- Brand: logo image kept; KONCHE letter-spacing tightened .18em → .02em (16.5px, compact 9px mark gap); header subtitle replaced sitewide (header only — footer untouched) "WATER TREATMENT TECHNOLOGY" → gray "Water Treatment" (9px, #8A979D), now displayed at every desktop width ≥780px (previously hidden below 1180px).
- Flat bar: header background solid #FFFFFF, backdrop blur removed, hairline #E3E9EB bottom border, no shadows; orange CTA unchanged (already flat).
- Nav unchanged from v11: seven items evenly distributed (space-between) between brand and CTA with the six gradient hairline separators; overlap-free verified again with the geometry probe at 9 widths (780–1600) including the new wider brand block — zero overlaps/overflows, subtitle renders as block everywhere in band.
- HTML subtitle edits on all 26 live pages; asset version v20260822-12; site_audit / validate_geo green; regression 49/49.

### Owner follow-up (same day): medium-pressure UV PROCESS FLOW removed + SVG archived

- The medium-pressure-uv-system page was the last one still carrying the PROCESS FLOW module (inline 1400×720 wp-svg strip). Removed per owner instruction, matching the other UV pages: the whole `<article id="process-flow">` block (8.9KB) and its "Process Flow" TOC entry are gone; zero residual references.
- Following the established practice, the diagram was archived before deletion as `assets-archive/process-flow/medium-pressure-uv-system-process-flow.svg` (standalone SVG extracted from the inline markup, archive now 13 files).
- regression_20260822.mjs archive assertion updated 12 → 13. dateModified already 2026-08-22. site_audit / validate_geo green; regression 49/49 PASS.

## Header visual polish — 2026-08-23 (pass 39, css v20260823-1)

Owner: layout is right, styling looked plain/cheap. No dedicated UI-design skill exists in the session, so the polish was done by hand (§34, ≥780px):

- Brand hierarchy: KONCHE 800-weight 17px ink wordmark over the tracked gray "Water Treatment" subtitle (8.5px, .09em).
- Nav labels: weight 600 in solid slate #3A464D (was washed-out light gray); hover/current in deep green #00763A with an 18px green underline accent that slides in on hover (pure color, no gradients/shadows — flat brief maintained).
- Separators softened: 16px tall, #C3CCD1 mid-tone gradient tips.
- CTA refined: uppercase tracked label (.1em), 2px radius, deep-orange #D96612 hover replacing the white-background hover.
- Verified: screenshots at 1440/1000 confirm bolder wordmark, solid labels, green active underline, refined button; geometry probe re-run at 5 widths — zero overlaps; site_audit/validate_geo green; regression 49/49.

## UI界面设计V1.0 — navigation accent system + layout/typography rhythm pass — 2026-08-24 (css v20260824-01)

Backup taken before the pass: `_site_backups/UI界面设计V1.0-2026-08-24.zip` (site tree minus node_modules/.git).
Scope honored: hero banners, headline copy and body description text on every page are untouched — verified by diffing five representative HTML files against the backup (identical after stripping the asset `?v=` string). Only `app/industrial.css` gained rules (§39–§41, appended; no earlier sections deleted) and all 26 real pages bumped `industrial.css?v=20260824-01`.

- **§39 Navigation accent system.** Earthy deep-green active treatment retired. Two-tone accent derived from the requested fresh muted green: `#34A688` for hover labels, underlines, CTA fills and graphic highlights; `#278060` (accent-ink) for small green rest-state text on light surfaces (AA); active page = soft dark-gray `#2C333A` block with white label and 14–16px horizontal padding so text never touches block edges. Desktop (≥780px) nav items are content-hugging 42px chips, vertically centered; hover shows a short animated green underline; active block is stable on hover. Product pages light the Products module via `:has(.dropdown-group-link.active)`. Dropdown panel: 6px radius, 10px padding, rows 10px 14px with 4px radius — current row solid #2C333A/white, hover row green tint + inset green bar. Drawer (≤779px) mirrors the language (gray active row, green hover). Header CTA and all non-hero CTA fills unified on #34A688 with #278060 hover. In-page active tabs (`.industry-tab.is-active`) join the #2C333A language.
- **§40 Interaction-accent harmonization.** All green hover/interactive states (text-link, footer links, breadcrumbs, TOC, quicknav, solutions index, carousel controls, form focus rings, consent accent) moved to #34A688; stale `rgba(244,122,34,…)` orange remnants replaced with accent-green equivalents; §35.6 signature water lines' green end-stop joins the accent. Static structural greens (#009944 card rules, bullets, eyebrow dashes) and every hero element keep their existing colors.
- **§41 Layout & typography rhythm.** Progressive `text-wrap: balance/pretty` on headings/paragraphs (hero excluded); long-form paragraph measure capped at 860px; card-grid gaps unified to an 18px step (24px feature cards); certificate cards re-padded 22/22/26 with title margins normalized; related cards 28/26; FAQ question/answer insets aligned (20/24 rhythm) on both FAQ families; table cells 14px 18px with 1.6 line-height; sticky TOC panel airier; buttons outside heroes tightened (12px arrow gap, 16×28 padding, 2px radius); mobile paddings tuned (tables 12/14, FAQ 18/20, inquiry form 24/20).
- Validation: five-page HTML diff vs backup identical (content preservation); headless-Chrome QA at 1440px and 375px — home header (Home active gray block + green CTA), Resources hover (green label + underline), product page (Products module gray block), Products dropdown (gray current row, green-tint hover row, no edge-touching text), mobile drawer (gray active row), hero unchanged (photo, green headline words, trust badges intact), capability CTA (#34A688 fill, comfortable padding).

## Content alignments, RO process-flow rewrite and site-wide green unification — 2026-08-24 (css v20260824-02)

Owner requests executed on top of the UI界面设计V1.0 pass (backup unchanged; hero banners/visuals untouched):

- **Customer-group phrasing unified sitewide.** The homepage QUICK TECHNICAL ANSWER answer, the homepage ABOUT KONCHE paragraph, about.html (body + JSON-LD) and every other occurrence now read "overseas EPC contractors, water treatment engineering integrators and project owners with equipment design, manufacturing, factory testing and technical support" — 47 normalized instances across all pages (idempotent regex, no double-prefixing).
- **Geo question widened:** "What water treatment systems does KONCHE provide?" → "What products and services does KONCHE provide?"; the answer keeps the equipment enumeration and now closes with the unified support sentence.
- **WHY PARTNER WITH US:** section slogan "Five advantages behind every KONCHE system, from factory floor to years of operation." removed; card 03 Project delivery capability now reads "Since 1997, we have designed and delivered water treatment systems for 10,000+ customers, covering electronics manufacturing, new energy, pharmaceuticals, food and drinking water, construction and other industries."
- **PRODUCT CAPABILITY cards:** "Energy" retitled "Energy consumption" on all six category pages (RO, electronic-grade UPW, containerized, laboratory UPW, UF, UV).
- **RO page technology comparison:** single-stage product-water reference "Conductivity up to 10 μS/cm" → "Conductivity ≤10 μS/cm".
- **RO page PROCESS FLOW → COMPLETE PROCESS FLOW:** module renamed and rebuilt as a process chain (Raw water tank → Pretreatment → Stage-1 RO → Stage-2 RO → UV → Pure water tank → Point of use) plus a §42-styled narrative paragraph (95%+ stage-1 salt rejection, ≤2 μS/cm stage-2 permeate, concentrate recycle, UV delivery). The old five-stage list is gone.
- **Green unified to #00763A everywhere** (§42): #009944 (legacy brand green), #34A688/#278060 (the §39 accent pair) and all green rgba() tints folded into the single value; filled-CTA hovers use filter:brightness(.85) for feedback; the two pale-green ticks on dark bands (#9fc99d) joined the §35 mist-blue dark-band accents; spare-parts inline form focus colors included; ::selection, focus rings and water-line end-stops follow. Nav active state remains the #2C333A gray block.

## RO complete-process-flow step cards — 2026-08-24 (css v20260824-03)

Owner request: render the treatment chain as horizontal step cards (node blocks + arrow connectors), keep green scarce, keep the narrative readable and clear of the PROJECT INQUIRY band.

- **§43 step cards:** `.process-chain` paragraph replaced by `.process-steps` — 7 white hairline node blocks (46px min-height, 3px radius, subtle shadow) joined by 6 gray `→` (U+2192 only) arrows; "Treatment process" label in #00763A caps above the row; headline keyword "treatment train." stays the only other green in the module. Desktop: single full-width row; mobile: flex-wrap onto multiple rows with no horizontal overflow (verified at 375px).
- **Key parameter:** "≤2 μS/cm" wrapped in `b.key-param` (weight 750, tabular figures, no-wrap) — Unicode ≤ and μ preserved verbatim.
- **Reading rhythm:** narrative line-height 1.78 desktop / 1.75 mobile; bottom clearance to the runtime-injected PROJECT INQUIRY band set to ≈1.6 line-heights (`.premium-demo .product-content` keeps its 0 bottom padding by design, so the clearance lives on the paragraph).
- Module headline/intro layout: `#process-flow` intro row collapses to a single column so the step row spans the container under the two-line headline.
- Verified headless at 1440px and 375px: 13/13 DOM checks (node/arrow counts, single row, colors, key-param, line-height, clearance, wrap, no overflow) plus visual screenshot review.

## Single/double-stage RO geo-answer figure + CSS comment repair — 2026-08-24 (css v20260824-05)

- **§44 geo-answer figure:** the single-double-stage-ro QUICK TECHNICAL ANSWER band returns to its two-column grid with the owner-supplied portrait system diagram (app/images/products/ro-double-stage-system-diagram.png, 1032×1382) in the right column — white hairline card, max-height 560px contain, copy column keeps its full measure (verified 653px, no overlap). The decorative corner circle (::after) is retired on figure-bearing bands (.geo-answer.has-figure). Mobile ≤860px: figure stacks below the copy, max-height 480/420px.
- **CSS repair:** an unterminated inline comment in the §43 margin-bottom rule (missing `*/`) made the parser swallow every rule after it — §43 step-card mobile rules and §44 were dead in the v=04 sheet (image unconstrained, circle still visible). Comment closed; comment-token audit now balanced (93/93); sheet re-verified green (image loads, two columns, no squeeze, circle gone, mobile stacked).
- Known pre-existing, out of scope this pass: at 375px this page's detail-card long content still reports scrollWidth 531 (clipped visually by the html overflow-x: clip guard); the local-preview-only preview-note badge also extends past the viewport. Neither is caused by the figure.

## Single/double-stage RO data alignment + working-principle module removed — 2026-08-24

Owner pass on single-double-stage-ro.html: operating pressure unified sitewide on this page as single stage 1.0–1.6 MPa / double stage 1.0–1.5 MPa — technical-data table (both columns), the citable-passage summary and the Key Components high-pressure-pump card now state the split explicitly. The WORKING PRINCIPLE module (four cards incl. "Single-pass separation" / "Second-pass polishing") was removed outright per owner decision, along with its "On this page" TOC entry; the detail-blocks now open at Key Components. Verified: zero occurrences of working-principle/Single-pass/Second-pass on disk and over the preview server; all four MPa mentions carry the new split values.

## Reclaimed Water Treatment System product page removed — 2026-08-24

Owner decision: the product introduction is retired from the site.

- products/reclaimed-water-treatment-system.html deleted.
- All inbound links removed across 24 pages: header mega-menu / mobile drawer entries, footer dropdown rows, floating quicknav rows, and related-system cards on drinking-water, single-double-stage-ro, uf-water-treatment-system, ozone-disinfection-system and others (57 anchors in total, removed as whole elements).
- JSON-LD cleaned: isRelatedTo/BreadcrumbList Product entries on drinking-water-system, ro-reverse-osmosis-system and uf-water-treatment-system; sitemap.xml <url> block and the llms.txt / llms-full.txt lines dropped.
- Verified zero references site-wide (html/xml/txt/js). Plain-text mentions of "reclaimed water" as a water-reuse concept in body copy remain intentionally. The orphaned hero image app/images/products/reclaimed-water-treatment-system.webp was left in place (unused, harmless).

## Electronic-grade page: unified capability CTA — 2026-08-24

Owner correction: the six PRODUCT DOCUMENTATION capability cards on electronic-grade-ultrapure-water-system.html still carried individual "buyer-card-link" CTAs. All six removed; one unified "Request a Quote" button (button-primary) now sits under the card grid in the same .product-capability-cta format as the RO page. Verified over the preview server: zero per-card links, one unified CTA, six cards intact.

## Single/double-stage RO: documentation passage collapsed — 2026-08-24

The long citable summary in the PRODUCT DOCUMENTATION intro on single-double-stage-ro.html now sits inside the same collapsible "Product documentation" disclosure (details/summary) used on the electronic-grade page, instead of rendering as an always-open paragraph. Content unchanged; verified served markup.

## Laboratory UPW page: empty equipment-documentation module removed — 2026-08-24

The placeholder EQUIPMENT DOCUMENTATION band on laboratory-ultrapure-water-system.html (heading "System documentation." + one-line description, no actual content) was deleted outright per owner request. The substantive "Product documentation" collapsible in the PRODUCT DOCUMENTATION intro is untouched. Verified served page: zero occurrences of the removed band.

## Laboratory UPW page: process paragraph into collapsible — 2026-08-24

The detailed process/spec paragraph on laboratory-ultrapure-water-system.html (PP + activated carbon + softening pretreatment, RO, EDI, 254/185 nm UV, UF; 18.2 MΩ·cm Type I, TOC <5 ppb, ≥5 MΩ·cm second grade; 5–100 L/h; GB/T 33087-2016 & ASTM D1193; 7-inch touch screen, alarms, cartridge-life prediction, <50 dB; typical applications) moved from the always-open PRODUCT DOCUMENTATION intro into the page's existing "Product documentation" collapsible as a second citable paragraph. Intro keeps the one-line summary. Verified served markup.

## Containerized water treatment page: full documentation module — 2026-08-24

Owner request: the page was content-thin; rebuild it from the official site's integrated-water product (konchewater.com/products/integrated-water.html), in the single-double-stage-ro documentation format. A complete PRODUCT DOCUMENTATION band (detail-container + detail-has-toc) was inserted between the PRODUCT CAPABILITY section and the gallery:

- Intro summary + "Product documentation" collapsible (long-form citable passage covering process, materials, dosing, standards, delivery model).
- Left TOC (On this page) with 8 anchors.
- CORE BENEFITS — 6 cards (integrated train & footprint −50%, ≤1 NTU outlet, ~60% faster install, 3–5× tube-settling efficiency, 5–500 m³/h coverage, corrosion-conscious build).
- TECHNICAL DATA — 12-row table (KCIW-5–500, 5–500 m³/h, ≤1 NTU, PAC+PAM 10–50 mg/L, 60° PP/FRP tubes, sand+anthracite, 2.5–5 m/h, 0.1–0.3 MPa, surface water 10–500 NTU, NaOCl/UV, CS/SS body).
- MODEL RANGE — KCIW-10/50/200/500 duties + custom row.
- INDUSTRY APPLICATIONS — rural drinking water, industrial process water, emergency, municipal, construction sites.
- SELECTION GUIDE — 4-step process list (water quality, duty, 1.2× peak factor, factory-direct sourcing).
- REFERENCE PROJECT — Ji'an Jiangxi rural drinking water (KCIW-50, 50 m³/h, 30–80 NTU feed → 0.5 NTU, GB 5749, 40 m², 15 days, 6,000 households) with 4-cell meta grid.
- MAINTENANCE — 5-row cycle table (daily backwash → 2–3-year media replacement).
- FAQ — 4 collapsible questions (sources, direct drinking, dosing, maintenance).

Verified 10/10 headless checks: TOC entries, all section ids, card/table/case/FAQ counts, collapsible, rendered blocks, anchor navigation. Existing hero, PRODUCT CAPABILITY cards, gallery and CTA untouched.

## Containerized page: WHY KONCHE added, gallery removed, collapsible repositioned — 2026-08-24

- WHY KONCHE module (4 cards: nearly 30 years of expertise / professional customization / controlled system quality / full-cycle technical service) appended after the FAQ inside detail-blocks, matching the single-double-stage-ro format; TOC gains a ninth "Why KONCHE" entry.
- The INSTALLED REFERENCES gallery section was deleted outright per owner request.
- The merged "Product documentation" collapsible moved from the band under QUICK TECHNICAL ANSWER to directly beneath the PRODUCT DOCUMENTATION intro paragraph ("KONCHE containerized water treatment plants integrate coagulation … 50% smaller than separate civil units."), ahead of the left TOC.
- Verified headless: gallery gone, band between intro and TOC, 4 why-cards after FAQ, 9 TOC entries with working anchor, single collapsible retained.

## Containerized page: geo-answer figure — 2026-08-24

The QUICK TECHNICAL ANSWER band on containerized-water-treatment-system.html now carries the owner-supplied containerized-RO diagram (app/images/products/containerized-ro-system-diagram.png, 1448×870 landscape) in a right-hand figure column, reusing the §44 has-figure pattern from single-double-stage-ro: two-column grid restored, copy column keeps its full measure, decorative corner circle removed, white hairline card at max-height 560px, mobile stacks below the copy. Verified headless at 1440px and 375px (loads, no overlap/squeeze, circle gone, no overflow).

## Geo-answer figures: white card background removed — 2026-08-24 (css v20260824-07)

Owner request: drop the white background behind the QUICK TECHNICAL ANSWER diagrams. The §44 .geo-answer-figure img now renders background transparent, no border, no padding, with mix-blend-mode: multiply (same technique the site already uses for .ind-photo-frame.ind-contain), so white-canvas equipment diagrams fuse into the mist-blue geo-answer band on containerized and single-double-stage-ro pages alike. Verified computed styles (transparent / 0 / multiply) and screenshot review: diagram blends into the band, still clearly readable.

## Containerized page layout repair — 2026-08-24 (css v20260824-08)

Regression from the collapsible repositioning: the .doc-collapsible band was inserted as a direct child of the detail-has-toc grid (220px TOC column + content column) without a column span, so auto-placement squeezed it into the 220px TOC column and displaced the TOC and every following block to the wrong cells — the whole documentation module appeared compressed to the right. §45.1 adds `.detail-has-toc .doc-collapsible {grid-column: 1 / -1;}`; verified headless: band spans the full row under the intro, TOC back in the left column, detail-blocks full width right of it, no horizontal overflow.

## Containerized layout root-cause fix — 2026-08-24 (follow-up to §45.1)

The real culprit behind the compressed documentation layout was not grid placement: a stray text fragment "v>" left after `</nav>` (an off-by-two slice in the earlier TOC update) became an anonymous grid item inside the detail-has-toc grid, occupied the content column, and pushed .detail-blocks into the 220px TOC column. Fragment removed; §45.1 span rule kept as hardening. Audited all pages for stray text after `</nav>`: none remain. Verified headless: TOC left, blocks full-width right, order intro → collapsible → TOC, no overflow.

## Laboratory menu entry flattened site-wide — 2026-08-24

Owner report: the Laboratory Ultrapure Water System menu entry showed two levels on the homepage but one level on product pages. Root cause: the 2026-08-22 pass-36 cleanup had flattened the duplicated self-referencing submenu only on product pages; the root-level pages (index header mega-menu, index footer, about, contact, privacy, resources) still carried `dropdown-group has-children` with a submenu that linked the very same page. All six occurrences now match the product-page shape — a single direct `dropdown-group` link. Audited the remaining site: zero duplicated laboratory submenu wrappers.

## UF pages merged into one — 2026-08-24

Owner request: merge the two UF product pages into one, following the containerized-page format. uf-water-treatment-system.html (Hollow-Fiber UF) was folded into uf-ultrafiltration-system.html and deleted (404):

- PRODUCT CAPABILITY rebuilt as six framework cards (What it is / How it is used / Where it fits / Effectiveness / Energy consumption / Cost) carrying the merged substance — PVDF 0.01 μm hollow-fiber modules, 1–200 m³/h, automatic backwash + air scouring + CEB, RO-membrane protection, 0.1–0.3 MPa / 0.3–0.7 kWh/m³, 2–5-year membrane life — with the unified Request-a-Quote CTA; the old four product-section cards removed.
- The hollow-fiber page's documentation module moved in whole (intro summary, 7-article detail-has-toc: Working Principle / Technical Data / Core Benefits / Applications / Maintenance / FAQ / Why KONCHE) plus its RELATED SYSTEMS band; the "Product documentation" collapsible merges both pages' long-form passages (two citable paragraphs) under the intro, §44/§45.1 styling reused.
- The UF-vs-RO TECHNOLOGY COMPARISON section and geo-answer band from the UF systems page retained.
- Sitewide link cleanup: 50+ anchors to the deleted page removed; 32 now-childless UF dropdown groups flattened to single-level direct links (same shape as Laboratory); JSON-LD breadcrumb/isRelatedTo entries re-pointed or removed; sitemap.xml and llms.txt/llms-full.txt lines dropped. Zero references remain.
- Verified headless: 6 cards + unified CTA, one collapsible with both passages, 7 TOC/section anchors, comparison retained, TOC-left/blocks-right layout intact, nav group single-level, no overflow.

## UF merge follow-up — 2026-08-24

Second-pass cleanup: the merged page's own header UF group (its link carries the `active` class, which the first flatten regex missed) plus 24 empty `.pq-subs` wrappers removed. Precise site-wide recheck: zero broken UF dropdown groups remain — earlier "REMAIN" hits were false positives from an over-broad proximity regex matching the adjacent UV group. Final verification on uf-ultrafiltration-system.html: 11/11 checks PASS.

## Products dropdown hover hardening — 2026-08-24 (css v20260824-09)

Owner report: moving the cursor toward the Water Treatment Consumables group made the whole Products dropdown vanish before its two third-level links could be clicked. Headless trajectories (1440px and 1366×725) could not force a close on the current build, pointing to transient hover blind spots in real hand movement — group borders and the layout shift while a sibling submenu animates open/closed. §46 hardening: the dropdown panel now closes with a 300ms grace delay (visibility transition-delay only on the closing state), submenu collapse waits 250ms before retracting, and scrollbar-gutter: stable keeps panel geometry fixed when a short viewport makes it scroll. Verified: panel opens, stays open across the Consumables group and both third-level links (hover targets confirmed via elementFromPoint), survives border jiggling near the bottom of the panel, and still closes cleanly once the cursor leaves the nav.

## UF page: geo-answer figure — 2026-08-24

The QUICK TECHNICAL ANSWER band on uf-ultrafiltration-system.html now carries the owner-supplied UF diagram (app/images/products/uf-system-diagram.png, 1086×1448 portrait) in a right-hand figure column via the §44 has-figure pattern: two-column grid, full-measure copy column, corner circle removed, transparent multiply-blended image (§44 v7 styling) at max-height 560px, mobile stacks below. Verified 4/4 at 1440px and 375px.

## Products dropdown: instant submenu reveal — 2026-08-24 (css v20260824-10)

Owner follow-up: the Consumables third-level links were still unreachable. Root cause found by comparing direct jumps (worked) with swept paths (failed): the animated max-height reveal/collapse made the single-column panel reshuffle mid-hover — as the UV group's submenu collapsed, the Consumables group slid away from under the stationary pointer, so its own submenu never opened and the panel appeared to vanish. §47 disables the submenu transition entirely on ≥780px (reveal and collapse are instant), keeping §46's panel close grace and scrollbar-gutter. Verified end-to-end on home and a product page with a slow 20px-step sweep through every group: panel stays open, Consumables submenu expands instantly, and real CDP clicks on both third-level links navigate to their pages.

## Products dropdown: always-visible submenus — 2026-08-24 (css v20260824-11)

Definitive fix for the unreachable Consumables third-level links. True root cause (reproduced by elementFromPoint at the dead spot): the collapsed panel was only ~284px tall — a cursor gliding from Products toward the last group left the panel's bottom edge before any submenu expanded it, the hover chain broke, the panel closed, and the pointer landed on the page hero with no menu left. §48 makes every dropdown-submenu permanently visible inside the single-column Products panel (max-height none, visible, left rule indent), so the full three-level tree is one stable hover surface — no nested hover timing to break. §46 close-grace and scrollbar-gutter retained; on short viewports the panel simply scrolls. Verified headless: panel opens at full tree height, all submenus visible without hover, complete slow sweep through the panel never closes it, Consumables group hover holds, and real clicks on General Water Treatment Consumables and UV Disinfection Consumables navigate to their pages.

## UF related-systems self-link removed — 2026-08-24

- Removed the `UF Ultrafiltration Systems` card from the merged UF page's `Continue your system selection` section because it linked back to the page currently being viewed.
- Retained the remaining downstream RO recommendation; no CSS change or cache-version bump was required.

## UF model table, laboratory related link and merge cleanup — 2026-08-24

- UF `TECHNOLOGY COMPARISON`: restored the blank UF column header as `PVDF hollow-fiber ultrafiltration (UF)` and refreshed the verification date.
- UF `MODEL RANGE`: added KC-UF-1/5, KC-UF-10/30, KC-UF-50/100 and KC-UF-200 with 1–5, 10–30, 50–100 and 200 m³/h capacities and translated application scopes from the official KONCHE UF equipment page (`konchewater.com/products/uf-water.html`); added the TOC entry.
- Laboratory ultrapure-water page: added a `RELATED SYSTEMS` card linking to `single-double-stage-ro.html`; removed the adjacent generic duplicate inquiry CTA so the page keeps its laboratory-specific CTA.
- Root-page product quick navigation: confirmed the Laboratory parent and child used the identical name and URL, then removed the redundant self-referencing child on index, about, contact, privacy and resources.
- UF merge residual repaired: removed the first stale CTA/footer/quicknav plus the premature `</body></html>` boundary, retaining one UF-specific CTA, one current footer, one current quicknav and one `pqPanel` ID. The remaining RO card label now matches the merged Single & Double Stage RO destination.
- No CSS change or cache-version bump was required.

## Homepage QUICK TECHNICAL ANSWER two-column redesign — 2026-08-24 (css v20260824-12)

- Rebuilt the homepage-only module as an image-free 50/50 B2B layout: a translucent `#E8F3E8` answer card on the left and a `TWO DIRECTIONS TO WORK WITH KONCHE` panel on the right, on `#F0F5F5`.
- The right panel contains two horizontal business cards: six linked standard/custom equipment tags plus an `#services` jump, and the requested spare-parts/component-sourcing copy and local service-page link.
- Preserved the sitewide single-green rule by using `#00763A` for all green accents rather than introducing additional greens.
- Responsive QA target: two main columns and the two business cards stack automatically at tablet/mobile widths; no images were added.

### Homepage two-column visibility follow-up — 2026-08-24 (css v20260824-13)

- Root cause of the module appearing unchanged in the in-app preview: its 843px content viewport fell below the original 1100px stacking breakpoint, so the new major columns rendered vertically.
- The 50/50 layout and the two horizontal business cards now remain side by side down to 761px; at 760px and below the full module stacks for mobile.
- Added a compact 761–1100px layout pass so both business cards remain readable at the in-app preview width.
- Applied the owner-specified accent values exactly: heading rule `#207845` and action-link text `#19663B`.

### Homepage business-card images — 2026-08-24 (css v20260824-14)

- Added the two owner-supplied images to the right-hand business cards, following the provided reference's image-above-copy structure.
- Converted the source PNG files to optimized WebP without changing their visual content: `quick-standard-custom-equipment.webp` and `quick-spare-parts-sourcing.webp`.
- Equipment uses a centered cover crop; sourcing uses a right-biased focal point so the specialist and inspected components remain visible.
- Added intrinsic dimensions, descriptive alt text, lazy loading, matching 16:10 media frames and reduced-motion handling.

### Homepage answer-card depth treatment — 2026-08-24 (css v20260824-15)

- Added a restrained right-biased green radial glow to the `What products and services does KONCHE provide?` card.
- Added two low-opacity, lightly rotated glass-panel layers inspired by the owner reference; content remains above the decoration and all layers ignore pointer input.
- Reduced the layer offsets and rotation on mobile so the treatment does not create overflow or visual noise.

## Homepage INDUSTRIES SERVED image cards — 2026-08-24 (css v20260824-16)

- Added all eight owner-supplied industry images in the numbered source order: electronics/semiconductors, PCB/displays, new energy/batteries, pharmaceuticals, food/beverage, chemical processing, power/boilers and laboratories/research.
- Converted the original ~2 MB PNG files to 1200px-wide WebP assets (about 60–132 KB each) for practical homepage loading, without changing the visual content.
- Rebuilt each industry module as a 16:9 image card with industry title and description below; added intrinsic dimensions, descriptive alt text, lazy loading and reduced-motion handling.
- Applied the requested local palette: `#207845` titles/media rule, `#F4F7F4` panel background and `#E0E6E0` card/panel borders.
- Responsive grid: 4 columns desktop, 2 columns tablet and 1 column below 560px.

## Homepage WHY PARTNER WITH US detail upgrade — 2026-08-24 (css v20260824-17)

- Preserved the existing six English titles/descriptions and desktop 3-column × 2-row structure.
- Added six dependency-free inline SVG icons in one 32px, 1.7px-stroke, round-cap linear style: factory, engineering gear, delivery checklist, technical-support engineer, supply-chain package and accountable-partner handshake.
- Upgraded `01–06` into 28px `#207845` circular badges; increased card padding to 32×28px, title to 18px/700 and body readability to 14px at 1.65 line-height.
- Replaced the old joined hairline grid with 20px breathing space, individual `#E7EBE7` borders, 8px radius, restrained shadow/lift and a 30px green closing rule.
- Strengthened the second slogan line `Backed for life.` in `#207845`; responsive layout remains 3 columns desktop, 2 tablet and 1 mobile.

## Drinking-water subtitle language cleanup — 2026-08-24

- Removed the Chinese parenthetical `（直饮水系统）` from the drinking-water product hero subtitle; the English `Direct Drinking Water System` label remains unchanged.

## Product-documentation placement and consumables copy cleanup — 2026-08-24

- Moved the UF system's complete `PRODUCT DOCUMENTATION` module directly behind `PRODUCT CAPABILITY`; `TECHNOLOGY COMPARISON` now follows the documentation module.
- Moved the ozone system's long introductory description into a collapsible `Product documentation` details panel.
- Removed the two specified delivery/selection paragraphs from the spare-parts page without changing the remaining consumables content.

## Homepage hero colour hierarchy — 2026-08-24 (css v20260824-18)

- Updated the homepage hero title to `#FFFFFF`, highlighted words to `#6EE7B7`, subtitle to `#B8EBD1`, body copy to `#D1D5DB`, and primary CTA to `#207845`.
- Scoped the overrides to the homepage hero only; retained the existing restrained brightness hover treatment and all original copy/layout.

## Homepage hero colour refinement — 2026-08-24 (css v20260824-19)

- Refined the homepage hero highlight and subtitle to `RGB(220, 253, 251)` / `#DCFDFB`, and the primary CTA to `RGB(0, 92, 67)` / `#005C43`.
- Retained the white main title and `#D1D5DB` body copy from the preceding hero update.

## Site-wide brand accent consolidation — 2026-08-24 (css v20260824-20)

- Unified the site's shared emphasis system—highlight words, primary buttons, status markers, rules, icons, focus states and related translucent accents—from the former `#00763A` family to `RGB(0, 92, 67)` / `#005C43`.
- Updated 205 solid-colour declarations and 62 translucent `rgba()` declarations across the shared stylesheets and the one page with inline accent styles.
- Bumped all 24 real HTML pages to `industrial.css?v=20260824-20`; intentional pale-green surfaces and the homepage hero's `#DCFDFB` light highlight remain unchanged.

## Homepage customer pathways module — 2026-08-24 (css v20260824-21)

- Added a three-card `WHO WE SUPPORT` module immediately before `WHY PARTNER WITH US`, targeting EPC/engineering integrators, distributors/importers/wholesalers and direct end-users with the approved English technical copy.
- Added five role-specific support points and a contact CTA to each card, plus a six-part shared service-commitment bar.
- Added restrained industrial styling: three-column desktop layout, 8px white cards, `#005C43` accent system, light-grey surface, subtle lift/shadow and stacked mobile layout.
- Added `→ 与我们的工程师聊聊您的项目` below the existing WHY PARTNER cards, linked to `contact.html`.

## Customer-pathways English cleanup — 2026-08-24 (css v20260824-22)

- Replaced every remaining Chinese phrase in the new customer-pathways module, shared service commitment and WHY PARTNER CTA with English copy.
- Removed all three card-level CTAs and their unused styles; retained only the single `Talk to Our Engineers About Your Project` CTA below WHY PARTNER.
- Reduced card minimum height and removed the former button spacing while preserving the three-column desktop and stacked mobile layouts.

## Reference-project UI system — 2026-08-24 (css v20260824-23)

- Changed the homepage customer-pathways heading to `To be an accountable partner.`
- Restyled all five `REFERENCE PROJECT` modules with `#E6F2EB` surfaces, 8px radius, expanded responsive padding, a short `#005C43` top rule and a stronger matching title.
- Preserved all English project copy; strengthened each existing project-summary sentence and added bold treatment to the containerized project's existing opening sentence.
- Rebuilt project metadata as a two-column desktop / one-column mobile grid with uppercase labels, `#223836` values and `#C8DDD6` separators; added a distinct visual state for the UV project's `Result` cell.
- Updated the five affected product pages to `industrial.css?v=20260824-23`.
## Homepage conversion sequence and RFQ CTA — 2026-08-24 (css v20260824-24)

- Added a second hero CTA, `Request a Quote`, linked to the contact page while retaining `Explore Products` as the primary product-path action.
- Reordered the homepage decision path to Quick Answer → Products/Services → Selected Reference Projects → Who We Support → Why KONCHE → Industries → Clients/Certifications → FAQ/Resources → Project Inquiry.
- Added three homepage reference-project summaries from existing product pages: industrial ultrapure water for electronics, in-line UV for food processing and containerized rural drinking-water treatment. Each card links to the full original project module.
- Added a compact three-card technical-resources entry before the inquiry module and retained About plus operating statistics as supporting proof within the Why KONCHE sequence.
- Replaced the homepage spare-parts card's hardcoded localhost URL with a deployable relative link and updated the homepage `dateModified` value.
## Homepage single-heading consolidation — 2026-08-24 (site-enhancements v20260824-2)

- Replaced each homepage module's former marketing H2 with its existing eyebrow label and removed the duplicate eyebrow, leaving one clear module heading.
- Applied the same homepage-only rule to the dynamically injected `PROJECT INQUIRY` section without changing inquiry headings on product or contact pages.
- Left the hero H1, internal panel headings, card titles and all body copy unchanged.
## Homepage Hero target, compact audience cards and uncropped project images — 2026-08-24 (css v20260824-25)

- Kept the Hero `Explore Products` target on the same-page `#services` anchor so it reaches OUR SERVICE without a reload.
- Fixed the actual click interception root cause by capping the desktop Products dropdown above the Hero CTA and disabling pointer hits on hidden menu/submenu states.
- Removed the three role-specific slogans, lead paragraphs and bullet lists from `WHO WE SUPPORT`; retained only the numbered customer types and added one section-level `Your Project, Our Technical Backbone` inquiry CTA to `contact.html`.
- Changed the three homepage project images from cropped `cover` presentation to centered `contain` inside 4:3 media frames, with hover scaling removed so the complete source image remains visible.
## Definitive responsive navigation rebuild — 2026-08-24 (css v20260824-27)

- Replaced the conflicting 780–1180px desktop-navigation override with a clear breakpoint: full navigation at 1181px and above, compact drawer navigation below 1181px.
- Increased full desktop navigation labels to 15px and drawer labels to 16px, with equalized spacing, stable single-line desktop labels and wrapping protection inside the drawer.
- Unified every navigation hover, focus, active state, submenu control and header CTA around the owner-specified `RGB(70,128,99)` accent, with one restrained highlight treatment.
- Rebuilt the compact header as brand + quote CTA + menu button, hiding the quote CTA below 640px to protect space; the opened menu uses a full-width, scroll-safe panel.
- Updated every real HTML page to the shared `industrial.css?v=20260824-27` asset version so navigation stays consistent across the site.
- Locked compact Products submenu labels at 14px so older high-specificity rules cannot reduce them to 12px.
## Homepage WHY KONCHE B2B value cards — 2026-08-25 (css v20260825-41)

- Preserved the six existing titles and matching line icons; positioned the green circular number at top left and the icon at top right.
- Added concise English value descriptions for overseas B2B buyers covering factory-direct supply, custom RO/UF/EDI engineering, project delivery, lifecycle support, spare-parts sourcing and single-point accountability.
- Standardized card padding at 32px 28px and grid gap at 24px; kept the section title left aligned.
- Set responsive layout to three columns on desktop, two on tablet and one on mobile.
## Homepage Hero message and key-figure reduction — 2026-08-25

- Replaced the Hero H1 with `Engineered water treatment systems built for your project.` while retaining the existing highlighted-keyword treatment.
- Kept the overseas-project capability paragraph in the right column.
- Retained only `30+ Years` and `10,000+ Customers`; removed the `18.2 MΩ·cm UPW` and `0.5–400 t/h capacity` figures.
- Corrected the pre-existing Hero container nesting so the capability paragraph and key figures render in the intended right-hand column on desktop.

## PROJECT INQUIRY role dropdown readability — 2026-08-25 (css v20260825-42)

- Changed the `You Are A` dropdown option text to near-black (`#111111`) on a white native option background.
- Scoped the change to `#inquiry-role` so other form fields and interaction states remain unchanged.

## Homepage certification credential cleanup — 2026-08-25

- Removed `3A Credit Enterprise` and `CE Marking` from the additional credentials list under `MANAGEMENT SYSTEM CERTIFICATIONS`.
- Kept `Invention Patents` and `Manufacturing since 1997`, with the list allowed to reflow naturally.

## UV consumables 10–1,000 W range and service-parts expansion — 2026-08-25

- Expanded `PRODUCT CATEGORIES` and `PRODUCT DATA` to state KONCHE's 10–1,000 W lamp-and-ballast supply range.
- Separated conventional low-pressure, low-pressure high-output / amalgam and medium-pressure lamps; clarified that a 1,000 W rating alone does not determine lamp pressure and selection must also use spectrum, electrical data, dimensions, cap and reactor model.
- Added G10Q wired ceramic lamp holders and more specific quartz-sleeve selection data.
- Added the owner-supplied lamp-cap, wired-holder and quartz-sleeve photographs to `UV SERVICE PARTS`.

## Homepage Hero OBM wording — 2026-08-25

- Replaced `30-year OEM manufacturer` with `30-year OBM manufacturer` in the Homepage Hero anchor line.
- Left the remaining Hero copy, actions, statistics and styling unchanged.

## Homepage selected-project city-name removal — 2026-08-25

- Removed `Shenzhen`, `Dongguan` and `Ji'an` from the three `SELECTED REFERENCE PROJECTS` homepage cards.
- Retained each project's industry, process, capacity and result information; product-page reference-project details remain unchanged.
